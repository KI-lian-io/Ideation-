Standard text field — 40px, warm canvas fill, hairline border, 2px mint focus ring.

```jsx
<Input label="Work email" placeholder="you@company.com" required />
<Input label="API key" error="This key is invalid" />
```

Supports `hint`, `error`, `required`, `iconLeft`. Focus turns the border 2px mint (the brand activation signal).
