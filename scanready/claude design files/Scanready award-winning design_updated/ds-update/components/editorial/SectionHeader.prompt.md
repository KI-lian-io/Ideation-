The folio head that opens every numbered marketing band — dimmed mono index, eyebrow, display-serif headline.

```jsx
<SectionHeader index="02" eyebrow="How it works" title="Three steps. One honest output." />
```

- Number bands sequentially 01…05; the ghost numeral pattern (readme → Banded rhythm) uses the same index.
- `align="center"` for the proof/CTA bands.
