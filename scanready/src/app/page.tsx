// Temporary placeholder — replaced by the full landing page in Plan 02.
// This is a Server Component (no 'use client') so Plan 02 can export metadata here.
export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 py-12">
      <p className="text-sm text-zinc-500">Landing coming soon.</p>
      <a
        href="/app"
        className="mt-4 text-sm font-semibold text-zinc-900 underline underline-offset-2 hover:text-zinc-600"
      >
        Open the tool →
      </a>
    </main>
  )
}
