---
phase: quick-260717-g8q
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: ["260717-g8q-soft-launch-readiness"]
files_modified:
  - scanready/src/lib/legal-data.ts
  - scanready/src/lib/payments-config.ts
  - scanready/src/app/app/page.tsx
  - scanready/src/app/agb/page.tsx
  - scanready/src/app/datenschutz/page.tsx

must_haves:
  truths:
    - "Impressum/legal pages render the founder identity (Kilian Hartmann, Teststraße 1, 22299 Hamburg, kilian.hartmann@liloplus.de) instead of the placeholder markers."
    - "When NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY is unset, the Humanizer+ CTA and both PDF-export purchase CTAs do not render in /app."
    - "When paketUnlocked is true, the PDF-export button still renders (already-paid free functionality) regardless of the payments flag."
    - "AGB and Datenschutz section numbering is 1..N contiguous in all four {paymentsEnabled} x {accountsEnabled} env combinations."
    - "No placeholder marker remains anywhere in scanready/src/, and no em-dash (U+2014) is introduced in any touched file."
  artifacts:
    - path: "scanready/src/lib/payments-config.ts"
      provides: "paymentsEnabled() client-safe env gate"
      exports: ["paymentsEnabled"]
    - path: "scanready/src/lib/legal-data.ts"
      provides: "LEGAL operator identity with real values + confirmation-note comment"
      contains: "Kilian Hartmann"
    - path: "scanready/src/app/app/page.tsx"
      provides: "Three paid CTAs gated behind paymentsEnabled()"
    - path: "scanready/src/app/agb/page.tsx"
      provides: "Paid AGB sections gated + contiguous renumbering"
    - path: "scanready/src/app/datenschutz/page.tsx"
      provides: "Paid Datenschutz sections gated + contiguous renumbering"
  key_links:
    - from: "scanready/src/app/app/page.tsx"
      to: "scanready/src/lib/payments-config.ts"
      via: "import { paymentsEnabled }"
      pattern: "paymentsEnabled"
    - from: "scanready/src/app/agb/page.tsx"
      to: "scanready/src/lib/payments-config.ts"
      via: "import { paymentsEnabled }"
      pattern: "paymentsEnabled"
    - from: "scanready/src/app/datenschutz/page.tsx"
      to: "scanready/src/lib/payments-config.ts"
      via: "import { paymentsEnabled }"
      pattern: "paymentsEnabled"
---

<objective>
Make the public site coherent for a payments-off soft launch and fill the operator identity used by the legally required pages.

Three coordinated changes in scanready/:
1. Fill the operator-identity placeholders in `src/lib/legal-data.ts` with founder-provided data and rewrite the header comment to flag the two values still needing confirmation.
2. Add a client-safe `paymentsEnabled()` gate and hide the dead-end paid CTAs in `/app` when it is false.
3. Gate the paid legal sections in AGB and Datenschutz behind `paymentsEnabled()`, keeping section numbering contiguous in every {payments} x {accounts} env combination.

Purpose: the deployed site must never advertise a paid feature that is not actually available, and must never describe processing (Stripe, purchases, Widerruf) that does not exist in the deployed configuration, while the always-free anonymous flow stays fully intact.
Output: five edited/created source files, all typechecking, testing, and building clean, with no placeholder markers left in src/ and no em-dash introduced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@scanready/src/lib/legal-data.ts
@scanready/src/lib/stripe.ts
@scanready/src/lib/supabase/config.ts
@scanready/src/app/agb/page.tsx
@scanready/src/app/datenschutz/page.tsx
@scanready/src/app/app/page.tsx

Key facts established during planning (do not re-derive):
- `src/lib/stripe.ts` imports the server-only `stripe` package at module scope, so `paymentsEnabled()` MUST live in its own tiny module, never in stripe.ts. New module: `src/lib/payments-config.ts`.
- The correct env var is `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` (used by HumanizerModal/PaketModal/PassModal via `loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY ?? '')`). Being NEXT_PUBLIC_, it is inlined into the client bundle at build time and readable server-side at runtime, so `paymentsEnabled()` works in both server components (agb/datenschutz pages) and the `'use client'` /app page.
- `accountsEnabled()` in `src/lib/supabase/config.ts` is the pattern to mirror (client-safe, fail-open, called inline).
- The test script (`npm test`) runs only lib-level node tests; none touch legal-data, the legal pages, or the /app CTAs, so these edits do not affect the existing 114-test suite as long as the LEGAL export shape is preserved.
- All five target files currently contain zero em-dash characters. Keep it that way.

GUARDRAILS (hard):
- NO em-dash (U+2014) anywhere. Use spaced hyphens or rewording. This applies to the new legal-data comment and the new module JSDoc.
- NO copy invention. Only hide/show existing legal blocks (whole `<section>` or an inline conditional on an existing sentence) and update the LEGAL values + its header comment. Do not write new legal sentences.
- Do NOT touch modal internals or account UI: HumanizerModal, PaketModal, PassModal, StorageGate, PassStatusChip, PassModal, SaveApplicationButton, AccountMenu, AccountProvider. StorageGate/PassModal are already gated behind accountsEnabled(); leave that alone.
- The anonymous free flow (paste CV, Lebenslauf, Anschreiben, copy, .txt download) must stay fully functional in every combination.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill LEGAL operator identity and add the client-safe paymentsEnabled() gate</name>
  <files>scanready/src/lib/legal-data.ts, scanready/src/lib/payments-config.ts</files>
  <action>
Two independent lib-level edits.

(1) In `scanready/src/lib/legal-data.ts`, replace the four placeholder values in the `LEGAL` object with the founder-provided data: `operatorName` becomes "Kilian Hartmann", `street` becomes "Teststraße 1", `city` becomes "22299 Hamburg", `email` becomes "kilian.hartmann@liloplus.de". Leave `country: "Deutschland"` and the entire `vatLine` field (value AND its inline JSDoc comment) exactly as they are.
Rewrite ONLY the top-of-file header block comment (the one above `export const LEGAL`). The rewritten comment must: keep the reference that these values back the legally required pages (Section 5 DDG, GDPR Art. 13); state that the placeholder markers are now filled with founder-provided data; and add two confirmation notes before production promotion: (a) the street value reads like a test address and must be confirmed as the real ladungsfähige Anschrift required by Section 5 DDG before production promotion, and (b) the email was defaulted from the founder profile and needs confirmation. Keep the pointer to docs/humanizer-golive.md. The new comment must contain zero em-dash characters and must NOT contain the old placeholder marker token. <!-- planner-discipline-allow: FOUNDER_TODO -->

(2) Create a NEW file `scanready/src/lib/payments-config.ts` exporting a single function `paymentsEnabled(): boolean` that returns `Boolean(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY)`. Give it a JSDoc header that mirrors the style and reasoning of `accountsEnabled()` in `src/lib/supabase/config.ts`: explain it is the single source of truth for whether the paid one-shot features (Humanizer+ and the Bewerbungspaket PDF export) are on; the same fail-open convention as supabase/config.ts and stripe.ts; that it lives in its own module rather than stripe.ts because stripe.ts imports the server-only `stripe` package (importing that from a `'use client'` component would pull server code into the browser bundle); and that it reads only a NEXT_PUBLIC_ variable so it is safe to import from both server and client code. Reference the full literal `process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` as a static member expression so Next inlines it. No em-dash in the JSDoc.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && npm test && npm run build && ! grep -rq "FOUNDER_TODO" src/ && ! grep -rq "$(printf '\xe2\x80\x94')" src/lib/legal-data.ts src/lib/payments-config.ts && grep -q "Kilian Hartmann" src/lib/legal-data.ts && grep -q "kilian.hartmann@liloplus.de" src/lib/legal-data.ts && grep -q "paymentsEnabled" src/lib/payments-config.ts</automated>
  </verify>
  <done>
LEGAL exports operatorName "Kilian Hartmann", street "Teststraße 1", city "22299 Hamburg", email "kilian.hartmann@liloplus.de", country "Deutschland" (unchanged), vatLine unchanged. Header comment rewritten with the two confirmation notes, no placeholder marker, no em-dash. `src/lib/payments-config.ts` exists and exports `paymentsEnabled()`. tsc, tests, and build pass; no placeholder marker anywhere in src/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hide the three dead-end paid CTAs in /app when payments are off</name>
  <files>scanready/src/app/app/page.tsx</files>
  <action>
Add `import { paymentsEnabled } from '@/lib/payments-config'` next to the existing `import { accountsEnabled } from '@/lib/supabase/config'` (near line 31).

Gate exactly three CTA buttons by wrapping each button element in a conditional expression. Mirror the existing inline `accountsEnabled()` usage style (call `paymentsEnabled()` inline in the condition). Do NOT alter the buttons' onClick/aria/className/label contents, and do NOT touch any sibling element.

(a) First PDF-export button (around line 1201, in the Lebenslauf result view; label expression `paketUnlocked ? t.exportPdfCta : t.paketLockedExportCta`, onClick `paketUnlocked ? onExportPdf(ortDatum) : onRequestPaket()`): wrap the whole `<button>...</button>` in `{(paketUnlocked || paymentsEnabled()) && ( ... )}`. This keeps the button when already unlocked (free export functionality) and when payments are on (working purchase CTA), and hides only the locked-state purchase CTA when payments are off. Leave the adjacent `{paketUnlocked && (...paketUnlockedBadge)}` and the `{paketUnlocked && (...ort-datum input block)}` untouched.

(b) Humanizer+ button (around line 1868; onClick `setHumanizerOpen(true)`, label `t.humanizerCta`): wrap the whole `<button>...</button>` in `{paymentsEnabled() && ( ... )}`. This is a pure purchase CTA, so it hides entirely when payments are off. The HumanizerModal mount elsewhere stays as-is (it becomes unreachable, which is correct).

(c) Second PDF-export button (around line 1900, in the Anschreiben result view; label `paketUnlocked ? t.exportPdfCta : t.paketLockedExportCta`, onClick `paketUnlocked ? onExportPdf() : onRequestPaket()`): wrap the whole `<button>...</button>` in `{(paketUnlocked || paymentsEnabled()) && ( ... )}`, same rationale as (a). Leave the `{paketUnlocked && (...exportPdfHint)}` sibling untouched.

Do not modify the price-anchor text, the txt-forewarning text, the over-limit hint, SaveApplicationButton, or any modal. Scope is exactly these three buttons plus the one import.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && npm test && npm run build && grep -q "import { paymentsEnabled } from '@/lib/payments-config'" src/app/app/page.tsx && [ "$(grep -c "paymentsEnabled()" src/app/app/page.tsx)" -ge 3 ] && ! grep -rq "$(printf '\xe2\x80\x94')" src/app/app/page.tsx</automated>
  </verify>
  <done>
`/app` imports paymentsEnabled. The Humanizer+ button and both PDF-export purchase CTAs are wrapped so they do not render when NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY is unset. The two PDF-export buttons still render when paketUnlocked is true (free already-paid export). No sibling elements, modals, or free-flow controls changed. tsc, tests, build pass; no em-dash added.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate paid legal sections in AGB + Datenschutz with contiguous renumbering</name>
  <files>scanready/src/app/agb/page.tsx, scanready/src/app/datenschutz/page.tsx</files>
  <action>
In BOTH files, add `import { paymentsEnabled } from '@/lib/payments-config'` next to the existing accountsEnabled import, and add `const payments = paymentsEnabled();` next to the existing `const accounts = accountsEnabled();`.

AGB (`src/app/agb/page.tsx`) - four edits, mirroring the existing `{accounts && (...)}` whole-section pattern and the existing ternary numbering:
- Section "1. Geltungsbereich und Anbieter": wrap the whole `<section>` in `{payments && ( ... )}`. Keep its "1." label and its inner `{accounts ? ... : ""}` body conditional unchanged.
- Section "2. Leistung und Preis (Einmalkäufe)": wrap the whole `<section>` in `{payments && ( ... )}`. Keep its "2." label.
- Section "3. Abonnement" (already wrapped in `{accounts && (...)}`): change its heading number from the hardcoded "3." to `{payments ? "3." : "1."}`. Leave the wrap and body unchanged.
- Section "Widerrufsbelehrung": wrap the whole `<section>` in `{payments && ( ... )}`. Its heading number stays `{accounts ? "4." : "3."}` (correct because it only renders when payments is true).
- Section "Gewährleistung und Haftung": leave it always-rendered (do NOT wrap). Change its heading number from `{accounts ? "5." : "4."}` to `{payments ? (accounts ? "5." : "4.") : (accounts ? "2." : "1.")}`.

Datenschutz (`src/app/datenschutz/page.tsx`) - keep sections 1, 2, 3, 4 always-rendered:
- Section "3. Cookies": leave the section rendered and its "3." label unchanged. Gate ONLY the Stripe-fraud-cookie sentence: wrap the existing sentence beginning "Erst wenn Sie eine kostenpflichtige Funktion (Humanizer+) nutzen," and ending "(Art. 6 Abs. 1 lit. b und f DSGVO)." in `{payments && " ...that exact existing sentence... "}`, mirroring the sibling `{accounts && " ..."}` clause. Preserve leading/trailing spacing so the remaining text reads naturally; keep the `<strong>keine Cookies</strong>` first sentence and the "Auch die in Abschnitt 4 ..." sentence outside the conditional. Do not rewrite any words.
- Section "5. Zahlungsabwicklung (Stripe)": wrap the whole `<section>` in `{(payments || accounts) && ( ... )}`. The parentheses around `(payments || accounts)` are REQUIRED: `&&` binds tighter than `||`, so an unparenthesised `payments || accounts && (...)` would render the boolean and hide the section when payments is true. Gate on payments-or-accounts (not payments alone) because the accounts-gated section 7 body cross-references "Abschnitt 5" for subscription payment processing and its generic body ("Kostenpflichtige Funktionen werden über Stripe ... abgewickelt") is accurate for the subscription path too. Keep its "5." label.
- Section "6. Ihre Rechte": leave always-rendered. Change its heading number from hardcoded "6." to `{(payments || accounts) ? "6." : "5."}`. Leave its inner `{accounts ? ... : ...}` body conditional unchanged.
- Section "7. Konto und gespeicherte Bewerbungen": leave its `{accounts && (...)}` wrap and its "7." label unchanged (whenever accounts is true, section 5 also renders, so it is always number 7).

No em-dash anywhere. No new sentences.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && npm test && npm run build && grep -q "import { paymentsEnabled } from '@/lib/payments-config'" src/app/agb/page.tsx && grep -q "import { paymentsEnabled } from '@/lib/payments-config'" src/app/datenschutz/page.tsx && grep -q "(payments || accounts) &&" src/app/datenschutz/page.tsx && ! grep -rq "$(printf '\xe2\x80\x94')" src/app/agb/page.tsx src/app/datenschutz/page.tsx</automated>
  </verify>
  <done>
Both pages import and compute `payments`. Paid AGB sections (Geltungsbereich, Leistung/Preis, Widerrufsbelehrung) render only when payments is on; Abonnement stays accounts-gated; Gewährleistung always renders. In Datenschutz the Stripe-cookie sentence is inline-gated on payments, the Zahlungsabwicklung section is gated on (payments || accounts), Ihre Rechte always renders. Section numbering is 1..N contiguous in all four env combinations (see enumeration below). No em-dash, no invented copy.

AGB numbering by {payments P, accounts A}:
- P=true,  A=true:  1 Geltungsbereich, 2 Leistung, 3 Abonnement, 4 Widerruf, 5 Gewährleistung -> 1,2,3,4,5
- P=true,  A=false: 1 Geltungsbereich, 2 Leistung, 3 Widerruf, 4 Gewährleistung -> 1,2,3,4
- P=false, A=true:  1 Abonnement, 2 Gewährleistung -> 1,2
- P=false, A=false: 1 Gewährleistung -> 1

Datenschutz numbering by {payments P, accounts A}:
- P=true,  A=true:  1 Verantwortlicher, 2 Verarbeitung, 3 Cookies, 4 Reichweitenmessung, 5 Zahlungsabwicklung, 6 Ihre Rechte, 7 Konto -> 1..7
- P=true,  A=false: 1,2,3,4,5 Zahlungsabwicklung,6 Ihre Rechte -> 1..6
- P=false, A=true:  1,2,3,4,5 Zahlungsabwicklung (via accounts),6 Ihre Rechte,7 Konto -> 1..7
- P=false, A=false: 1,2,3,4, (no Zahlungsabwicklung), 5 Ihre Rechte -> 1..5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/runtime env -> rendered page | `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` presence decides which CTAs and legal sections render |
| client CTA -> server payment route | CTA visibility is a UX gate only; server routes keep their own `isStripeConfigured()` guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g8q-01 | Information Disclosure | LEGAL.email / LEGAL.street in legal-data.ts | accept | Operator postal address and contact email are legally mandated public identity (Section 5 DDG), not secrets. Street value explicitly flagged in-comment for founder confirmation before production promotion. |
| T-g8q-02 | Tampering / Elevation | paymentsEnabled() CTA gating | mitigate | Gating only hides client CTAs; the server routes (/api/humanizer/*, /api/humanize, /api/paket/*) retain their existing isStripeConfigured() guards unchanged, so a hidden or forged CTA cannot bypass server-side payment checks. |
| T-g8q-03 | Information Disclosure | AGB/Datenschutz gated sections | mitigate | Legal text is shown/hidden to match deployed config so the live notice never describes non-existent processing; the Datenschutz Abschnitt-5 cross-reference is preserved via the (payments || accounts) gate; numbering verified contiguous in all four env combinations. |
| T-g8q-SC | Tampering | npm/pip/cargo installs | accept | No package installs. Only one new local module (payments-config.ts) and edits to existing files. No Package Legitimacy Gate required. |
</threat_model>

<verification>
Run inside scanready/ after all three tasks:
- `npx tsc --noEmit` passes.
- `npm test` passes (existing lib suite unaffected).
- `npm run build` passes (Next 16 production build).
- `! grep -rq "FOUNDER_TODO" src/` (no placeholder markers remain).
- `! grep -rq "$(printf '\xe2\x80\x94')" src/lib/legal-data.ts src/lib/payments-config.ts src/app/app/page.tsx src/app/agb/page.tsx src/app/datenschutz/page.tsx` (no em-dash introduced).
- Manual reasoning check: walk the four {payments} x {accounts} combinations against the enumeration in Task 3 `<done>` and confirm each page's visible headings number 1..N with no gap.
</verification>

<success_criteria>
- LEGAL carries the four founder-provided values; country and vatLine unchanged; header comment rewritten with the ladungsfähige-Anschrift and email confirmation notes, no placeholder marker, no em-dash.
- `paymentsEnabled()` exists in its own client-safe module and is the only gate added.
- Humanizer+ and both locked-state PDF-export CTAs are hidden when payments are off; already-unlocked PDF export still works; free anonymous flow untouched.
- AGB and Datenschutz hide paid sections when payments are off, keep accounts sections on accounts, and number contiguously in all four env combinations.
- tsc, tests, and build all green; no placeholder marker in src/; no em-dash in touched files.
</success_criteria>

<output>
Create `.planning/quick/260717-g8q-soft-launch-readiness-fill-impressum-leg/260717-g8q-SUMMARY.md` when done.
</output>
