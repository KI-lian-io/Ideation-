---
phase: quick-260701-l9h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 6 analytics no longer appears as an active/pending roadmap phase — it is backlogged"
    - "TRUST-02 and OPS-01 are classified as deferred v2 requirements, not v1 phase-mapped requirements"
    - "STATE.md reflects the v1.0 active roadmap as phases 1-5, 100% complete"
    - "CLAUDE.md handoff no longer lists a pending 'push to claude.ai/design' next step"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "Analytics reqs relocated to v2 backlog section; coverage count corrected to 22"
    - path: .planning/ROADMAP.md
      provides: "Phase 6 moved to a new Backlog section; active roadmap = phases 1-5"
    - path: .planning/STATE.md
      provides: "Frontmatter + prose reflecting feature-complete v1 with analytics backlogged"
    - path: CLAUDE.md
      provides: "Design-sync follow-up + suggested-first-message rewritten (no stale push step)"
  key_links: []
---

<objective>
Reclassify the Phase 6 analytics funnel (PostHog) from an active/pending roadmap phase to a backlog item, applying the project's already-established backlog convention consistently across the three `.planning/` status files. Separately, remove the now-stale "push the design system to claude.ai/design" next step from the root `CLAUDE.md` handoff doc, since the DesignSync export already exists locally (`./ScanReady Design System/`) and was reconciled into the app (commits `7ea7b6d`, `65a10f3`).

Purpose: Keep the project's source-of-truth status files honest and consistent. The v1.0 milestone is feature-complete (phases 1-5); analytics is a deferred follow-up, not "the current phase." The handoff doc should not point the next session at work that is already done.

Output: Edited `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and root `CLAUDE.md`. Documentation only — no `scanready/` source changes; no deletion of design-system directories; no re-litigation of the analytics feature or design decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

# Boundary note: this is a status/classification change only. Do NOT delete
# `ScanReady Design System/` or `scanready/design-system/`. Do NOT touch any
# scanready/ source file. Do NOT re-open the analytics feature or design decisions.
# Use Edit (scoped replacements) on all four files — never Write over an existing file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backlog analytics in REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Apply the existing v2-backlog convention to the two analytics requirements and remove Phase 6 from the active roadmap.

REQUIREMENTS.md:
1. Remove the TRUST-02 bullet from the "### Trust and Compliance (TRUST)" section (the `- [ ] **TRUST-02**: Analytics capture funnel events only...` line).
2. Remove the OPS-01 bullet from the "### Analytics and Deploy (OPS)" section (the `- [ ] **OPS-01**: Funnel events fire...` line).
3. Under "## v2 Requirements" (after the "Deferred. Tracked, not in the current roadmap." line and its existing FMT/PAY/ENH subsections), add a new subsection "### Analytics (deferred)" containing the two relocated bullets, keeping their existing IDs but dropping the phase mapping and the checkbox-style formatting used by other v2 items — use plain `- **ID**: ...` bullets matching FMT/PAY/ENH. Preserve the full requirement text (TRUST-02: funnel events only, never CV/PII, with the PostHog `autocapture:false`, `ip:false`, `person_profiles:'identified_only'` settings; OPS-01: funnel events fire with the five event names `start`, `parse_done`, `letter_done`, `copy`, `download`).
4. In the "## Traceability" mapping table, remove the two rows `| TRUST-02 | Phase 6 | Pending |` and `| OPS-01 | Phase 6 | Pending |`.
5. Update the "**Coverage:**" block: change "v1 requirements: 24 total" to "22 total" and "Mapped to phases: 24" to "Mapped to phases: 22" (2 requirements moved out of v1 scope). Leave the "Unmapped: 0" line unchanged.
6. Update the trailing "*Last updated: ...*" line with today's date (2026-07-01) and a short reason: analytics (TRUST-02, OPS-01) backlogged to v2 — v1.0 milestone active roadmap is phases 1-5, all complete.

ROADMAP.md:
7. In "## Overview", change the last sentence ("Phase 6 adds the analytics funnel as a low-priority post-MVP follow-up.") to state the analytics funnel is backlogged rather than an active phase — pull it into a future milestone if analytics becomes a priority.
8. In the "## Phases" checklist, remove the `- [ ] **Phase 6: Analytics (post-launch)** - ...` line.
9. Cut the entire "### Phase 6: Analytics (post-launch)" spec block from "## Phase Details" (goal, mode, depends_on, requirements, success criteria, plans). Do NOT delete this content — move it verbatim into a new "## Backlog" section added at the END of the file (after the "## Progress" table), with a lead sentence such as: "Not scheduled — pull into a future milestone if analytics becomes a priority." Keep the goal / success criteria / depends_on / requirements intact inside the Backlog section.
10. In the "## Progress" table, remove the active row `| 6. Analytics (post-launch) | 0/TBD | Not started | - |`.
  </action>
  <verify>
    <automated>cd /Users/kilianhartmann/Documents/Claude/Projects/ideation && grep -q '### Analytics (deferred)' .planning/REQUIREMENTS.md && grep -q '22 total' .planning/REQUIREMENTS.md && ! grep -q 'TRUST-02 | Phase 6' .planning/REQUIREMENTS.md && ! grep -q 'OPS-01 | Phase 6' .planning/REQUIREMENTS.md && grep -q '## Backlog' .planning/ROADMAP.md && ! grep -q '0/TBD' .planning/ROADMAP.md && echo PASS</automated>
  </verify>
  <done>
- REQUIREMENTS.md: TRUST-02 and OPS-01 bullets removed from their v1 TRUST/OPS sections and relocated under a new "### Analytics (deferred)" subsection in "## v2 Requirements", with IDs and full text preserved.
- REQUIREMENTS.md: both `... | Phase 6 | Pending |` rows removed from Traceability; coverage reads "22 total" / "Mapped to phases: 22"; Last-updated line dated 2026-07-01 with reason.
- ROADMAP.md: Overview last sentence reframes analytics as backlogged; the Phase 6 checklist line and the `0/TBD` Progress row are gone; the Phase 6 spec content is preserved verbatim under a new "## Backlog" section at the end of the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md status and CLAUDE.md handoff</name>
  <files>.planning/STATE.md, CLAUDE.md</files>
  <action>
Bring the mutable status file and the human-facing handoff doc in line with the backlog reclassification (Task 1) and drop the stale design-push step.

STATE.md — frontmatter:
1. Set `current_phase: 5` and `current_phase_name: distribution-operationalization` (the last completed active phase; there is no active Phase 6 anymore). Set `status: complete` (v1.0 active roadmap is feature-complete pending backlog only). Optionally update `stopped_at` / `last_activity_desc` to reflect "v1.0 feature-complete; analytics backlogged".
2. In `progress:`, set `total_phases: 5`, `completed_phases: 5`, and `percent: 100`. Leave `total_plans: 14` / `completed_plans: 14` as-is (all 14 plans across phases 1-5 are complete).
3. Update `last_updated` to an ISO timestamp for 2026-07-01.

STATE.md — prose:
4. In "## Current Position", replace the "Phase: 6 — Analytics (post-launch)" / "Plan: Not started" / "Status: Executing Phase 05" / "Progress: [...] 0%" block so it states the v1.0 milestone is feature-complete (phases 1-5 done), with analytics backlogged rather than the current position. Show progress as 100% (e.g. `[██████████] 100%`). Update the "Last activity" line to 2026-07-01 with a matching description. Optionally align "**Current focus:**" in the Project Reference block (currently "Phase 05 — distribution-operationalization") to reflect feature-complete / backlog-only.
5. In the "## Deferred Items" table (the same table listing FMT-01/PAY-01/ENH etc.), add a row for the analytics funnel: Category `v2`, Item `Analytics funnel — funnel events (OPS-01) + PostHog zero-PII config (TRUST-02)`, Status `Deferred`, Deferred At `2026-07-01`. Add a second row for the paid test follow-on: Category `Blocked`, Item `GTM-03 paid conversion-test execution (run the test)`, Status `Blocked on analytics`, Deferred At `2026-07-01` — with a note that the GTM-03 *spec* is complete (Phase 5) and only *running* the paid test is blocked on the analytics funnel per `05-03-PLAN.md`'s existing BLOCKED flag. (This is a note on an already-complete requirement's follow-on action, not a new roadmap item.)

CLAUDE.md — Part B (drop stale claude.ai/design push step):
6. Rewrite the "**design-sync follow-up:**" bullet in the "## Design system — Warm Editorial Broadsheet" section. Replace the "Pushing to claude.ai/design is blocked in web Claude Code (needs interactive /design-login)... Then expand the overview card into per-component cards." text with a statement that the full DesignSync export already exists locally at `./ScanReady Design System/` (tokens/, components/, templates/, guidelines/, SKILL.md) and was already reconciled into the live app (commits `7ea7b6d`, `65a10f3`) — so there is no pending push action. Keep the pointer to `scanready/design-system/` as the in-app preview library if useful, but frame it as done, not pending.
7. In the "## Suggested first message for the new session" blockquote, drop option "(a) push the design system to claude.ai/design from an interactive terminal via `/design-login` + `scanready/design-system/`". Re-letter the remaining options: what was "(b) build the optional Phase 6 analytics funnel..." becomes the first option and must note it is now backlogged/optional (per the REQUIREMENTS/ROADMAP reclassification in Task 1) — no longer an active phase; and what was "(c) start distribution from `distribution/`" becomes the second option, otherwise unchanged. Keep the closing guidance ("clear the native-speaker German quality gate before going public, and `gh auth switch --user KI-lian-io` before pushing").
  </action>
  <verify>
    <automated>cd /Users/kilianhartmann/Documents/Claude/Projects/ideation && grep -q 'total_phases: 5' .planning/STATE.md && grep -q 'percent: 100' .planning/STATE.md && grep -q '100%' .planning/STATE.md && ! grep -q 'design-login' CLAUDE.md && ! grep -q 'push the design system to claude.ai/design' CLAUDE.md && grep -q 'ScanReady Design System' CLAUDE.md && echo PASS</automated>
  </verify>
  <done>
- STATE.md frontmatter: `current_phase: 5`, `current_phase_name: distribution-operationalization`, `status: complete`; `progress.total_phases: 5`, `completed_phases: 5`, `percent: 100`; `last_updated` set to 2026-07-01.
- STATE.md "## Current Position": no longer names Phase 6 as current; states the v1.0 milestone is feature-complete (phases 1-5) with analytics backlogged; progress shows 100%.
- STATE.md "## Deferred Items": has a row for the analytics funnel (TRUST-02, OPS-01) and a row noting GTM-03 paid-test *execution* is blocked on analytics (spec itself complete in Phase 5).
- CLAUDE.md: no remaining "/design-login" or "push the design system to claude.ai/design" text; the design-sync bullet states the export exists locally and was reconciled into the app; the Suggested-first-message list has option (a) removed, analytics reframed as backlogged/optional, and distribution retained.
  </done>
</task>

</tasks>

<verification>
Documentation-only consistency checks after both tasks:

1. Backlog convention applied consistently: `grep -n 'Analytics (deferred)' .planning/REQUIREMENTS.md` and `grep -n '## Backlog' .planning/ROADMAP.md` both return a hit; TRUST-02/OPS-01 no longer appear as v1 phase-mapped rows.
2. Counts consistent: REQUIREMENTS.md coverage reads 22; STATE.md frontmatter reads `total_phases: 5` / `percent: 100`; ROADMAP.md has no `0/TBD` Progress row and no `- [ ] **Phase 6` checklist line.
3. No stale design-push references remain: `grep -rn 'design-login\|push the design system to claude.ai/design' CLAUDE.md` returns nothing.
4. Boundary respected: `git status --porcelain scanready/` shows no changes; `ScanReady Design System/` and `scanready/design-system/` still exist on disk.
</verification>

<success_criteria>
- Phase 6 analytics is fully reclassified from active/pending to backlog across REQUIREMENTS.md, ROADMAP.md, and STATE.md, using the project's existing v2-backlog convention.
- The Phase 6 spec content is preserved (not deleted) in a ROADMAP "## Backlog" section.
- v1.0 milestone status reads feature-complete (phases 1-5, 100%) with analytics and the GTM-03 paid-test execution tracked as deferred/blocked items.
- CLAUDE.md no longer directs the next session to push the design system to claude.ai/design; both stale references are rewritten to reflect the already-reconciled local export.
- No `scanready/` source files changed; no design-system directories deleted.
</success_criteria>

<output>
Create `.planning/quick/260701-l9h-backlog-phase-6-analytics-drop-stale-cla/260701-l9h-SUMMARY.md` when done
</output>
