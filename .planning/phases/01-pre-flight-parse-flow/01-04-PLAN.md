---
phase: 01-pre-flight-parse-flow
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - scanready/src/components/SkillChips.tsx
  - scanready/src/components/NormGapPanel.tsx
  - scanready/src/components/LebenslaufEditor.tsx
  - scanready/src/app/page.tsx
autonomous: true
requirements: [LL-02, LL-03]
must_haves:
  truths:
    - "Skills render as editable chips grouped under German category headings"
    - "User can add/remove chips, add/remove categories, and edit category names and chip text"
    - "Language levels are chosen from a German-convention dropdown, not free text"
    - "normGapNotes render in a collapsible 'what changed & why' block below the Lebenslauf"
    - "photoAdvice renders in its own clearly-labelled optional-photo callout near the personal-data block"
  artifacts:
    - path: "scanready/src/components/SkillChips.tsx"
      provides: "categorized editable skill chips + German language-level dropdown constant"
      exports: ["SkillChips", "GERMAN_LANGUAGE_LEVELS"]
      min_lines: 40
    - path: "scanready/src/components/NormGapPanel.tsx"
      provides: "collapsible normGapNotes block + photoAdvice callout"
      exports: ["NormGapPanel"]
      min_lines: 20
  key_links:
    - from: "scanready/src/components/LebenslaufEditor.tsx"
      to: "scanready/src/components/SkillChips.tsx"
      via: "editor renders SkillChips for the Kenntnisse section and the language dropdown for Sprachen"
      pattern: "SkillChips"
    - from: "scanready/src/app/page.tsx"
      to: "scanready/src/components/NormGapPanel.tsx"
      via: "result phase renders NormGapPanel from lebenslauf.normGapNotes + photoAdvice"
      pattern: "NormGapPanel"
    - from: "scanready/src/components/SkillChips.tsx"
      to: "scanready/src/app/page.tsx"
      via: "chip + category + language edits dispatch skill/language reducer actions"
      pattern: "ADD_SKILL_CATEGORY"
---

<objective>
Complete the parse flow's German-norm UI: categorized editable skill chips (D-09 UI), the German-convention language-level dropdown (D-10 UI), the collapsible bilingual "what changed & why" panel rendered from `normGapNotes[]` (D-05 / LL-02), and the optional-photo callout rendered from `photoAdvice` (D-06 / LL-03). This consumes the categorized schema shape and German-level vocabulary produced by Plan 02 and slots into the editor shell from Plan 03.

Covers LL-02 (bilingual norm-gap panel, pure client render, no API change) and LL-03 (nuanced, never-mandated photo guidance), plus the UI halves of D-09 and D-10.

Purpose: These are the exact norm gaps the tool exists to fix (uncategorized skills, "fluent/native/B2" language wording) plus the trust-building "why it matters" education — the visible payoff of the phase.
Output: `SkillChips.tsx` (categorized chips + `GERMAN_LANGUAGE_LEVELS` + the level dropdown), `NormGapPanel.tsx` (collapsible notes + photo callout), the editor's Kenntnisse/Sprachen sections upgraded to use them, the skill/language reducer actions wired, and `page.tsx` rendering the panel below the Lebenslauf.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-flight-parse-flow/01-CONTEXT.md
@.planning/phases/01-pre-flight-parse-flow/01-RESEARCH.md
@.planning/phases/01-pre-flight-parse-flow/01-PATTERNS.md
@scanready/.claude/CLAUDE.md
@scanready/AGENTS.md
@scanready/src/lib/schema.ts
@scanready/src/components/LebenslaufEditor.tsx
@scanready/src/components/EditableField.tsx
@scanready/src/app/page.tsx
</context>

## Phase Goal

**As a** an expat job-seeker, **I want to** paste my US/UK résumé and get an editable, norm-correct German Lebenslauf with bilingual "what changed & why" notes, **so that** I can submit a CV a German recruiter takes seriously in the 8-second scan.

(Plan 04 delivers the categorized skills, German language levels, and the bilingual "what changed & why" notes + photo guidance — completing the phase's user story.)

## Artifacts this phase produces (Plan 04 portion)

| Symbol / artifact | File | Kind |
|---|---|---|
| `SkillChips` component | `scanready/src/components/SkillChips.tsx` | new component |
| `GERMAN_LANGUAGE_LEVELS` constant + level `<select>` | `scanready/src/components/SkillChips.tsx` | new constant + UI |
| `NormGapPanel` component (collapsible notes + photo callout) | `scanready/src/components/NormGapPanel.tsx` | new component |
| Skill-category reducer actions (`UPDATE_SKILL`, `ADD_SKILL`, `REMOVE_SKILL`, `ADD_SKILL_CATEGORY`, `REMOVE_SKILL_CATEGORY`, `UPDATE_SKILL_CATEGORY_NAME`) | `scanready/src/app/page.tsx` | reducer extension |

<tasks>

<task type="auto">
  <name>Task 1: Categorized skill chips + German language-level dropdown, wired through skill/language reducer actions (D-09 UI, D-10 UI)</name>
  <files>scanready/src/components/SkillChips.tsx, scanready/src/components/LebenslaufEditor.tsx, scanready/src/app/page.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/ (REQUIRED before component/app code per scanready/AGENTS.md)
    - scanready/src/lib/schema.ts (the `SkillCategory` = `{ category, skills }` type from Plan 02 and the `languages` = `{ language, level }` shape)
    - scanready/src/components/EditableField.tsx (reused for editable category names and chip text)
    - scanready/src/components/LebenslaufEditor.tsx (Plan 03 — the Kenntnisse + Sprachen sections render minimally today; replace them with SkillChips + the level dropdown)
    - scanready/src/app/page.tsx (Plan 03 reducer — extend the action union with skill-category cases; language actions UPDATE_LANGUAGE/ADD_LANGUAGE/REMOVE_LANGUAGE already exist from Plan 03)
    - .planning/phases/01-pre-flight-parse-flow/01-PATTERNS.md §"scanready/src/components/SkillChips.tsx" (chip group pattern, editable category label, per-chip remove, "+ add skill"/"+ add category", `GERMAN_LANGUAGE_LEVELS` constant)
    - .planning/phases/01-pre-flight-parse-flow/01-RESEARCH.md §"German Skill Categories (D-09)", §"Language Level Dropdown (D-10)" (the `<select>` with the five German levels + the empty "— Niveau wählen —" option), and the `LebenslaufAction` skill-category cases
    - .planning/phases/01-pre-flight-parse-flow/01-CONTEXT.md D-09, D-10
  </read_first>
  <action>
    Extend the reducer in `scanready/src/app/page.tsx` with the skill-category action cases from RESEARCH: `UPDATE_SKILL` (catIndex, skillIndex, value), `ADD_SKILL` (catIndex), `REMOVE_SKILL` (catIndex, skillIndex), `ADD_SKILL_CATEGORY`, `REMOVE_SKILL_CATEGORY` (catIndex), and `UPDATE_SKILL_CATEGORY_NAME` (catIndex, value) to edit a category label. Each case is a pure immutable update of `lebenslauf.skills` (an array of `{ category, skills }`). `ADD_SKILL` appends an empty chip; `ADD_SKILL_CATEGORY` appends `{ category: '', skills: [] }` so the empty category name renders as a fillable blank.

    Create `scanready/src/components/SkillChips.tsx` as a `'use client'` component that renders `skills: SkillCategory[]` as grouped chip sections: each category heading is an `EditableField` (editable label, dispatches `UPDATE_SKILL_CATEGORY_NAME`); each chip is an `EditableField` for the text plus an inline `×` remove button (dispatches `REMOVE_SKILL`); a "+ Kenntnis hinzufügen" button per category dispatches `ADD_SKILL`; a "+ Kategorie hinzufügen" button dispatches `ADD_SKILL_CATEGORY`; each category gets a remove (`REMOVE_SKILL_CATEGORY`) control. Export the `GERMAN_LANGUAGE_LEVELS` constant `['Muttersprache','Verhandlungssicher','Fließend','Gute Kenntnisse','Grundkenntnisse']`. Also provide the language-level `<select>` (an empty default option labelled "— Niveau wählen —" plus the five levels) used for each language row, dispatching `UPDATE_LANGUAGE` with field `level` (D-10); the language name itself stays an `EditableField`. Props receive the relevant slice + `dispatch`. Render all chip/category/language text as escaped JSX text/controlled inputs only — never inject raw HTML strings (the React raw-HTML escape hatch is prohibited; see threat model).

    Update `scanready/src/components/LebenslaufEditor.tsx` so the Kenntnisse section renders `SkillChips` and the Sprachen section renders each language as an `EditableField` (name) + the German-level `<select>` (from SkillChips' export or a shared spot), replacing the minimal Plan-03 render. The categorized skills and languages also serialize correctly through `toPlainText` (already category-aware from Plan 03; confirm category labels appear in copy output).
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && head -1 src/components/SkillChips.tsx | grep -q "use client" && grep -q "GERMAN_LANGUAGE_LEVELS" src/components/SkillChips.tsx && grep -q "Muttersprache" src/components/SkillChips.tsx && grep -q "Grundkenntnisse" src/components/SkillChips.tsx && grep -q "SkillChips" src/components/LebenslaufEditor.tsx && grep -q "ADD_SKILL_CATEGORY" src/app/page.tsx && grep -q "UPDATE_SKILL" src/app/page.tsx && ! grep -q "dangerouslySetInnerHTML" src/components/SkillChips.tsx</automated>
    <human-check>Run `npm run dev`, parse a sample CV, scroll to Kenntnisse: confirm skills appear as chips grouped under German category headings; add/remove a chip, add/remove a category, edit a category name; in Sprachen, set a language level via the dropdown (five German options); click Copy and confirm category labels + German levels appear in the pasted text.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Skills render as editable chips grouped under German category headings (D-09 UI)
    - Chips and categories can be added/removed; category names and chip text are editable; all via pure reducer updates
    - `GERMAN_LANGUAGE_LEVELS` exports the five German levels; the language-level `<select>` offers exactly those plus an empty default and dispatches `UPDATE_LANGUAGE` field `level` (D-10 UI)
    - `LebenslaufEditor` Kenntnisse section uses `SkillChips`; Sprachen uses the level dropdown
    - Categorized skills + German levels appear correctly in `toPlainText` copy output
    - No React raw-HTML injection in `SkillChips.tsx` (verify negative-greps source)
  </acceptance_criteria>
  <done>Kenntnisse renders as editable categorized chips and Sprachen uses the German-level dropdown, both wired through pure reducer actions and reflected in copy output.</done>
</task>

<task type="auto">
  <name>Task 2: Collapsible norm-gap panel (LL-02 / D-05) + optional-photo callout (LL-03 / D-06)</name>
  <files>scanready/src/components/NormGapPanel.tsx, scanready/src/components/LebenslaufEditor.tsx, scanready/src/app/page.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/ (REQUIRED before component/app code per scanready/AGENTS.md)
    - scanready/src/lib/schema.ts (`normGapNotes: string[]` and `photoAdvice: string` — the fields rendered here)
    - scanready/src/app/page.tsx (result phase — where the panel mounts below the editor)
    - scanready/src/components/LebenslaufEditor.tsx (the personal-data block, near which the photo callout sits per D-06)
    - .planning/phases/01-pre-flight-parse-flow/01-PATTERNS.md §"scanready/src/components/NormGapPanel.tsx" (native `<details>`/`<summary>` collapsible pattern, notes list, separate photo callout)
    - .planning/phases/01-pre-flight-parse-flow/01-CONTEXT.md D-05 (collapsible block below the Lebenslauf, CV stays the hero), D-06 (own clearly-labelled "Photo (optional)" callout near the personal-data block, framed optional-per-AGG, never mandated), and the guardrail "photo guidance is nuanced — never mandate"
    - .planning/phases/01-pre-flight-parse-flow/01-RESEARCH.md §"Security Domain" (XSS via field content — React escapes by default; the raw-HTML escape hatch is prohibited)
  </read_first>
  <action>
    Create `scanready/src/components/NormGapPanel.tsx` as a component rendering the bilingual "what changed & why" notes. Use a native HTML `<details>`/`<summary>` collapsible (no library) titled in German (e.g. "Was hat sich geändert & warum?") with the note count; the body renders `normGapNotes[]` as a list (D-05 / LL-02). This is a pure client render from the existing parse response — no API call. Local open/closed state only (use `<details>` or a local `useState`); do NOT put panel open-state in the reducer.

    Render `photoAdvice` as its OWN clearly-labelled callout (e.g. "Foto (optional)") — separate from the notes list (D-06 / LL-03). The copy must frame the photo as legally optional under the AGG but commonly expected, leaving the choice to the user; it must NOT mandate a photo and must NOT imply the tool processes/uploads a photo. Place this callout near the personal-data block (in `LebenslaufEditor.tsx` next to the `personal` section render), not buried in the notes panel.

    Wire `scanready/src/app/page.tsx` result phase to render `NormGapPanel` (fed `lebenslauf.normGapNotes`) BELOW the `LebenslaufEditor` so the CV stays the hero (D-05). Pass `photoAdvice` to where the callout renders near the personal block. Notes/advice are model-produced text — render them as escaped JSX text nodes only; never inject raw HTML strings (the React raw-HTML escape hatch is prohibited; see threat model).
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && grep -qE "export (function|const) NormGapPanel" src/components/NormGapPanel.tsx && grep -q "normGapNotes" src/components/NormGapPanel.tsx && grep -qE "<details|useState" src/components/NormGapPanel.tsx && grep -q "NormGapPanel" src/app/page.tsx && grep -q "photoAdvice" src/components/LebenslaufEditor.tsx && ! grep -q "dangerouslySetInnerHTML" src/components/NormGapPanel.tsx</automated>
    <human-check>Run `npm run dev`, parse a sample CV: confirm a collapsible "what changed & why" block sits below the Lebenslauf and expands to show one-line notes; confirm a clearly-labelled optional-photo callout sits near the personal-data block with non-mandating, AGG-framed wording.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `NormGapPanel` renders `normGapNotes[]` inside a collapsible block placed below the Lebenslauf (D-05 / LL-02)
    - The panel is a pure client render from the parse response — no additional API call
    - `photoAdvice` renders in its own labelled optional callout near the personal-data block, framed optional-per-AGG and never mandated, with no implication of photo upload/processing (D-06 / LL-03)
    - Panel open/closed state is local, not stored in the reducer
    - No React raw-HTML injection in `NormGapPanel.tsx` (verify negative-greps source)
  </acceptance_criteria>
  <done>A collapsible bilingual norm-gap panel renders below the Lebenslauf and a nuanced optional-photo callout sits near the personal-data block — both pure client renders from the existing parse response.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Block on: high severity. Pure client rendering of model-produced strings (`normGapNotes`, `photoAdvice`) plus categorized-skill editing of in-memory state; no new server surface, no persistence.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parse response -> React render | Model-produced `normGapNotes`/`photoAdvice` and skill text are rendered into the page |
| user chip/category/language edits -> in-memory state | User text stays in `useReducer` memory |

## STRIDE Threat Register

<!-- planner-discipline-allow: dangerouslySetInnerHTML -->

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | XSS via rendering `normGapNotes`/`photoAdvice`/skill text | mitigate (high) | React/JSX escapes by default; all values render as text nodes / controlled inputs. `dangerouslySetInnerHTML` PROHIBITED. Enforced by negative grep in verify. |
| T-01-11 | Repudiation/Information Disclosure | Photo callout implying upload/processing of a user photo | mitigate | Copy frames photo as optional guidance only; the tool does not accept, upload, or process a photo (consistent with the "guidance, not processing" guardrail and zero-retention). Verified in acceptance review of callout wording. |
| T-01-SC | Tampering | dependency installs | accept | No new packages; collapsible uses native `<details>`, chips are hand-rolled. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- Skills render as editable categorized chips; language levels via the German dropdown; both reflected in copy output.
- Collapsible norm-gap panel renders `normGapNotes[]` below the Lebenslauf; optional-photo callout near the personal block, never mandated.
- No React raw-HTML injection (verify negative-greps source); no new dependencies; pure client render (no extra API call).
</verification>

<success_criteria>
The parse flow is visually and functionally complete for Phase 1: skills are editable German-categorized chips, language levels use German CV vocabulary, the bilingual "what changed & why" notes render in a collapsible block below the hero Lebenslauf, and nuanced optional-photo guidance sits near the personal-data block — satisfying LL-02 and LL-03 with the D-09/D-10 UI.
</success_criteria>

<output>
Create `.planning/phases/01-pre-flight-parse-flow/01-04-SUMMARY.md` when done.
</output>
