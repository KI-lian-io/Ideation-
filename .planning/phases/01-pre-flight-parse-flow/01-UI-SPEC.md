---
phase: 01
slug: pre-flight-parse-flow
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-22
---

# Phase 01 — UI Design Contract

> Visual and interaction contract for Plans 01-03 and 01-04.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

**Scope note:** This contract covers the _functional_ UI for Phase 1 (the WYSIWYG Lebenslauf editor, norm-gap panel, photo callout, zero-retention trust line, loading skeleton). The _conversion-oriented_ visual design pass (landing page, brand palette, trust-forward marketing polish, "not generic AI-purple") is explicitly deferred to Phase 4. Executors must extend — not contradict — the patterns already present in `scanready/src/app/page.tsx`.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — hand-rolled Tailwind only | CLAUDE.md "What NOT to Add" + no `components.json` found |
| Preset | not applicable | — |
| Component library | none — no Radix, no shadcn, no headless UI | Locked |
| Icon library | none — inline SVG only (lock icon already defined in `page.tsx` lines 62–78) | CLAUDE.md: no new icon libraries |
| Font | Geist Sans (variable `--font-geist-sans` via `next/font/google`) | `layout.tsx` lines 5–10 |
| Mono font | Geist Mono (variable `--font-geist-mono`) — use for code/date fields only | `layout.tsx` lines 11–13 |

**Registry:** shadcn not initialized; no third-party registries. Registry safety gate: not applicable.

---

## Spacing Scale

Tailwind v4 utility values in use. All values are multiples of 4.

| Token | Tailwind | Value | Usage |
|-------|----------|-------|-------|
| xs | `gap-1` / `px-0.5` | 4px | Icon-to-text gaps, chip remove button padding, EditableField hover padding (`px-0.5`) |
| sm | `gap-1.5` / `p-1.5` / `gap-2` | 6–8px | Zero-retention line icon gap (`gap-1.5`), inline control spacing |
| md | `gap-4` / `px-4 py-3` | 16px | Form element internal padding (textarea, input), section gaps in editor |
| lg | `gap-6` / `p-4` | 16–24px | Card/callout internal padding (`p-4`), result-view vertical gap (`gap-6`) |
| xl | `py-12 px-6` / `sm:px-12` | 24–48px | Page outer padding (existing in `page.tsx` line 358) |

Exceptions:
- Touch-target minimum for ×/↑/↓ control buttons: 24px rendered, 44px tap target via `min-h-[44px] min-w-[44px]` or padding expansion. Applies to all entry-level controls in `LebenslaufEditor`.
- Textarea resize handle: `resize-y` permitted; `rows={18}` for input, `rows={12}` for junk retry (already set in `page.tsx`).

---

## Typography

Three sizes, two weights. Extend nothing beyond this set for Phase 1.

| Role | Size | Tailwind | Weight | Tailwind | Line Height | Usage |
|------|------|----------|--------|----------|-------------|-------|
| Body / field text | 14px | `text-sm` | 400 (regular) | `font-normal` | 1.5 | CV field content, norm-gap notes, photo advice copy, skeleton status text |
| Label / meta | 12px | `text-xs` | 500 (medium) | `font-medium` | 1.4 | Section headings in uppercase (`tracking-widest uppercase text-zinc-400`), skill category labels, language level dropdown label |
| Heading | 24px | `text-2xl` | 600 (semibold) | `font-semibold` | 1.2 | Page-level h1 ("Convert your CV…") and Lebenslauf name display — already set in `page.tsx` lines 98–99 and 174–176 |
| CTA / button | 14px | `text-sm` | 500 (medium) | `font-medium` | — (button, single line) | All buttons: primary CTA, secondary "Start over", "Try again", chip controls |

**Hard constraints (must not add):**
- No `text-lg`, `text-xl`, `text-3xl`, or any size outside the three declared sizes.
- No `font-bold` (700) anywhere in Phase 1 UI. Two weights only: 400 and medium/semibold (500–600).
- Date fields rendered with mono font for readability: add `font-mono` to `EditableField` when `onBlurFormat={softFormatDate}` is passed.

---

## Color

Source: `globals.css` + `page.tsx` Tailwind usage. Zinc neutral scale is the surface. No accent hue until Phase 4 (design pass).

| Role | Light mode | Dark mode | Tailwind class | Usage |
|------|-----------|-----------|----------------|-------|
| Dominant surface (60%) | `#f4f4f5` (zinc-100) | `#000000` (black) | `bg-zinc-50` / `dark:bg-black` | Page background — already on outer div `page.tsx` line 357 |
| Secondary surface (30%) | `#ffffff` (white) | `#000000` (black) | `bg-white dark:bg-black` | Main content card (`max-w-3xl` panel) — `page.tsx` line 358 |
| Elevated card | `#f9fafb` (zinc-50) | `#18181b` (zinc-900) | `bg-zinc-50 dark:bg-zinc-900` | Entry cards inside the editor, result blocks — matches existing `page.tsx` line 187 |
| Border subtle | `#e4e4e7` (zinc-200) | `#3f3f46` (zinc-700) | `border-zinc-200 dark:border-zinc-700` | Textarea borders, card outlines, button outlines |
| Text primary | `#18181b` (zinc-900) | `#fafafa` (zinc-50) | `text-zinc-900 dark:text-zinc-50` | All body content, field values |
| Text muted | `#71717a` (zinc-500) | `#a1a1aa` (zinc-400) | `text-zinc-500 dark:text-zinc-400` | Zero-retention line, secondary labels, empty-field placeholders, skeleton status |
| Text disabled / placeholder | `#a1a1aa` (zinc-400) | `#52525b` (zinc-600) | `text-zinc-400 dark:text-zinc-600` | Textarea placeholder, fillable blank prompt text |

**Semantic colors (reserved uses only):**

| Role | Value | Tailwind | Reserved for |
|------|-------|----------|-------------|
| Accent (10%) — edit focus | `#60a5fa` (blue-400) | `border-blue-400` | EditableField active input border-bottom only — signals "this field is being edited". Nothing else uses blue in Phase 1. |
| Edit hover | `#eff6ff` (blue-50) | `hover:bg-blue-50` | EditableField display span hover state — the 1-px hover affordance on editable text |
| Destructive | `#ef4444` (red-500) | `text-red-700 dark:text-red-400` / `bg-red-50 dark:bg-red-950/20` | Error state callout box only — `ErrorView` in `page.tsx` lines 216–219 |
| Warning / junk | `#f59e0b` (amber-500) | `text-amber-700 dark:text-amber-400` / `bg-amber-50 dark:bg-amber-950/20` | Junk-paste message box only — `JunkView` in `page.tsx` lines 244–252 |
| Photo callout | `#f4f4f5` (zinc-100) | `bg-zinc-100 dark:bg-zinc-800` | Optional-photo callout background (distinct from error/warning; neutral) |

**Accent reserved for:**
1. EditableField active input: `border-b border-blue-400` (bottom border on the active `<input>` or `<textarea>`)
2. EditableField hover: `hover:bg-blue-50 rounded` on the display `<span>`
3. Nothing else. Do not use blue on buttons, headers, chips, or any other element in Phase 1.

**Primary CTA button:** `bg-zinc-900 text-white hover:bg-zinc-700 dark:bg-zinc-100 dark:text-zinc-900 dark:hover:bg-zinc-300` — already set in `page.tsx` line 125. All primary buttons follow this contract.

---

## Component Inventory

Components this phase produces. Each must follow the class conventions above exactly.

### `EditableField`
- **Display state:** `<span className="cursor-pointer hover:bg-blue-50 rounded px-0.5 {className}">`. Value rendered as a text node. Null value renders placeholder in `text-zinc-400 italic`.
- **Active (editing) state:** `<input>` or `<textarea>` with `border-b border-blue-400 outline-none bg-transparent w-full {className}`. No box border — bottom border only signals edit mode.
- **Date variant:** add `font-mono` class to both states.
- **Commit triggers:** blur and Enter (single-line), blur only (multiline).
- **Cancel trigger:** Escape key — reverts to original value.
- **Focus:** `useEffect` auto-focuses the input when `editing` flips to `true`.
- **XSS rule:** no `contentEditable`, no `dangerouslySetInnerHTML`, ever.

### `LebenslaufEditor` — section-level layout
- Max width: inherits `max-w-3xl` from page container — no additional max-width.
- Section header: `<p className="text-xs font-medium uppercase tracking-widest text-zinc-400 mb-2">` — matches existing `page.tsx` line 190.
- Section card (experience/education entry): `rounded-lg border border-zinc-100 bg-zinc-50 p-4 dark:border-zinc-800 dark:bg-zinc-900` — matches existing `page.tsx` line 187.
- On-hover controls (×, ↑, ↓): `opacity-0 group-hover:opacity-100 transition-opacity` on a container that carries `className="group relative"`. Buttons: `text-zinc-400 hover:text-zinc-700 text-sm px-1.5 py-1` — minimum 24px rendered, touch target expanded via padding.
- Add affordance ("+ add experience", "+ Kenntnis hinzufügen"): `text-sm text-zinc-400 hover:text-zinc-600 cursor-pointer` inline link-style button.
- Section ↑/↓ reorder buttons: `text-xs text-zinc-400 hover:text-zinc-700` — appear at section edge, always visible (not hover-gated), because section reorder is a deliberate navigation action.

### `SkillChips`
- Chip wrapper: `flex flex-wrap gap-2`.
- Individual chip: `flex items-center gap-1 rounded-full border border-zinc-200 bg-zinc-50 px-3 py-1 text-sm dark:border-zinc-700 dark:bg-zinc-800` — the EditableField inside the chip is `text-sm font-normal`.
- Remove button on chip: `text-zinc-400 hover:text-zinc-700 text-xs ml-1` with `aria-label="Kenntnis entfernen"`.
- Category label: `text-xs font-medium uppercase tracking-widest text-zinc-500 mt-4 mb-1` — mirrors section header style.
- Language level `<select>`: `border-0 bg-transparent text-sm text-zinc-700 dark:text-zinc-300 focus:outline-none cursor-pointer`. Default option: "— Niveau wählen —" with value `""`. Five options: Muttersprache, Verhandlungssicher, Fließend, Gute Kenntnisse, Grundkenntnisse.

### `NormGapPanel`
- Container: rendered below `LebenslaufEditor` in `page.tsx`, separated by `mt-8 border-t border-zinc-100 dark:border-zinc-800 pt-6`.
- Collapsible: use native `<details>`/`<summary>` — no library, no custom state. `<summary className="cursor-pointer text-sm font-medium text-zinc-600 hover:text-zinc-900 dark:text-zinc-400 dark:hover:text-zinc-100 list-none flex items-center justify-between">`.
- Summary text: "Was hat sich geändert & warum? ({n} Hinweise)" — German label, English note count for scannability.
- Notes list: `<ul className="mt-3 space-y-1.5 text-sm text-zinc-600 dark:text-zinc-400">` — each note as `<li>` text node. No HTML injection.
- Photo callout: **outside** `NormGapPanel`, placed inside `LebenslaufEditor` adjacent to the personal-data section. `<div className="mt-4 rounded-lg border border-zinc-200 bg-zinc-100 px-4 py-3 dark:border-zinc-700 dark:bg-zinc-800">`. Label: `<p className="text-xs font-medium uppercase tracking-widest text-zinc-400 mb-1">Foto (optional)</p>`. Body: `text-sm text-zinc-600 dark:text-zinc-400`. Content must frame photo as optional under AGG, expected in practice — user's choice. Never mandate; never imply upload/processing.

### Loading skeleton (`LoadingView` — already in `page.tsx`)
- Pattern locked by D-15: `animate-pulse` pulsing blocks in zinc-200/zinc-700. Already implemented at `page.tsx` lines 136–156.
- Do not replace with a stepped progress bar. Do not add percentage text.
- Status label: `text-sm text-zinc-400 dark:text-zinc-500` — "Converting your CV to a German Lebenslauf…"

### Zero-retention line (`InputView` — already in `page.tsx`)
- Pattern locked by D-14: already at `page.tsx` lines 107–111.
- Inline `<p>` with lock SVG icon, `text-sm text-zinc-500`, no border, no background box.
- Copy locked: "Your CV is never stored or used for training — processing is stateless and zero-retention."
- Do not redesign this into a heavy bordered notice, a modal, or a banner.

---

## States and Interaction Contracts

All five phases of the `AppState` machine must be visibly distinct. The executor must not merge or omit any state.

| Phase | Visual indicator | Interaction allowed |
|-------|-----------------|---------------------|
| `input` | Full textarea + primary CTA | Text entry, submit |
| `loading` | Pulsing skeleton blocks + status text | None (submit disabled) |
| `result` | WYSIWYG editor + copy + start-over | All editor mutations, clipboard copy, reset |
| `error` | Red callout box + retry button | Edit pasted text, retry |
| `junk` | Amber callout box + preserved textarea + retry button | Edit pasted text (preserved per D-17), retry |

### Inline editing interaction contract
- **Click** any `EditableField` display span → swaps to `<input>` or `<textarea>`.
- **Blur** → commits value; runs `onBlurFormat` if provided (dates only).
- **Enter** (single-line) → commits value.
- **Escape** → cancels edit; reverts to pre-edit value.
- **Empty field (null/blank)** → display span shows placeholder text in `text-zinc-400 italic` style (fillable blank per D-08).
- **Focus ring**: the active input's bottom-border blue is the only focus indicator needed. Do not add `ring-*` or `outline` on top.

### Add/remove/reorder controls
- Entry controls (×, ↑, ↓) appear via `group-hover:opacity-100` on the entry card.
- Section ↑/↓ buttons are always visible (not hover-gated).
- No drag-and-drop in Phase 1 (deferred to Phase 4 design polish per RESEARCH.md).
- All "add" affordances are text-style links, not icon-only buttons, so the action is legible.

### Copy button
- Label: "Lebenslauf kopieren" (German, since the output is German).
- State: shows "Kopiert ✓" for 1500ms after clipboard write, then reverts. Implement with `useState` local flag — do not put copy state in the reducer.
- Error path: if `navigator.clipboard.writeText` throws, show `"Kopieren fehlgeschlagen — bitte manuell auswählen."` inline below the button for 3000ms.

### Start over button
- Already in `page.tsx` line 180: "Start over / paste a new CV". Keep this English copy — it is in the input-phase context where the user is English-speaking.
- Dispatches `RESET` → returns to `initialState`. No confirmation dialog (D-16: stateless, nothing is persisted so there is nothing to lose that the user cares to protect).

---

## Copywriting Contract

All copy is locked for Phase 1 functional flow. Marketing/landing copy is Phase 4.

| Element | Copy | Notes |
|---------|------|-------|
| Page h1 | "Convert your CV to a German Lebenslauf" | Already in `page.tsx` line 98 — do not change |
| Subtitle | "Paste your US or UK resume below. We will reformat it to a norm-correct German tabellarischer Lebenslauf, grounded strictly in your real CV facts." | Already in `page.tsx` lines 101–104 — do not change |
| Zero-retention line | "Your CV is never stored or used for training — processing is stateless and zero-retention." | D-14 / locked — do not change |
| Primary CTA | "Convert to Lebenslauf" | Already in `page.tsx` line 128 — do not change |
| Loading status | "Converting your CV to a German Lebenslauf…" | Already in `page.tsx` line 152 — do not change |
| Copy CTA | "Lebenslauf kopieren" | German — the user is now looking at a German document |
| Copy success | "Kopiert ✓" | Transient 1500ms state |
| Copy failure | "Kopieren fehlgeschlagen — bitte manuell auswählen." | Transient 3000ms state |
| Start over | "Start over / paste a new CV" | English — user is back at the input step |
| Error heading | "Something went wrong" | Already in `page.tsx` line 216 — do not change |
| Error body | Pass through `errorMessage` from reducer (already set in `page.tsx` lines 297–299, 336–340) | Do not use a single hardcoded string |
| Retry button | "Try again" | Already in `page.tsx` line 222 |
| Junk heading | "That did not look like a CV" | Already in `page.tsx` line 243 — do not change |
| Junk body | "We could not find a recognisable name and work or education history. Try pasting more of your resume — include your name, contact info, and at least one job or degree." | Already in `page.tsx` lines 245–249 — do not change |
| Norm-gap panel summary | "Was hat sich geändert & warum? ({n} Hinweise)" | German — the panel is part of the German-norm education |
| Photo callout label | "Foto (optional)" | German; must include "(optional)" |
| Photo callout body | Use `photoAdvice` string from parse response verbatim — render as a text node. The model produces appropriate nuanced framing per the system prompt. | Do not hardcode photo copy — the model generates context-aware advice |
| Fillable blank prompts | "+ Vorname, Nachname", "+ Telefonnummer", "+ Adresse", "+ E-Mail-Adresse", "+ Datum", "+ Unternehmen", "+ Standort", "+ Aufgabe hinzufügen", "+ Kenntnis hinzufügen", "+ Kategorie hinzufügen", "+ Sprache hinzufügen" | German — these blanks are inside the German Lebenslauf editor |
| Section labels (German) | "Persönliche Daten", "Berufserfahrung", "Bildung", "Kenntnisse", "Sprachen" | German DIN section names. Used as section headings in the editor and in `toPlainText` serialization |
| Language dropdown default | "— Niveau wählen —" | German; value `""` |
| Add experience entry | "+ Berufserfahrung hinzufügen" | German |
| Add education entry | "+ Bildungsabschluss hinzufügen" | German |
| Add language | "+ Sprache hinzufügen" | German |

**Destructive actions in Phase 1:**
- Remove experience entry (×): no confirmation dialog. Entry can be re-added. The action is reversible within the session (ADD_EXPERIENCE restores a blank entry). No destructive confirmation required.
- Remove education entry (×): same — no confirmation.
- Remove skill chip (×): same — no confirmation.
- Remove skill category (×): no confirmation — data loss of chips within that category. The executor should show a subtle inline warning: "Kategorie und alle Kenntnisse entfernen?" with a second tap/click to confirm. Use a local `useState` "pending delete" flag on the category; do NOT put this in the reducer.
- Remove language row (×): no confirmation — reversible.

---

## Accessibility

Minimum requirements for Phase 1 (WCAG 2.1 AA functional floor — full audit is Phase 4).

| Control | Requirement |
|---------|-------------|
| `<textarea>` | `aria-label="Resume text"` — already set `page.tsx` line 119 |
| `LoadingView` | `role="status" aria-label="Parsing your CV, please wait"` — already set `page.tsx` line 135 |
| Copy button | `aria-label="Lebenslauf in Zwischenablage kopieren"` |
| Remove buttons | `aria-label="Eintrag entfernen"` / `"Kenntnis entfernen"` / `"Kategorie entfernen"` (context-specific) |
| Reorder buttons ↑/↓ | `aria-label="Abschnitt nach oben"` / `"Abschnitt nach unten"` (for section-level), `"Eintrag nach oben"` / `"Eintrag nach unten"` (for entry-level) |
| `<details>`/`<summary>` (NormGapPanel) | Native HTML semantics — no ARIA additions needed |
| Language `<select>` | `aria-label="Sprachniveau"` |
| EditableField active input | `aria-label` must be passed or inferred from context (e.g., the field name). At minimum, the surrounding section heading provides context. |
| All interactive inline buttons | Must be `<button>` elements, not `<span onClick>`. Keyboard focusable by default. |
| Color contrast | All `text-zinc-400` on white fails AA (3.1:1). Use `text-zinc-500` (`#71717a`) as the minimum for any text users must read. `text-zinc-400` is permitted only for placeholder/decorative text. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not required |
| third-party | none | not applicable |

No new packages introduced in Phase 1 (per CLAUDE.md "What NOT to Add" and RESEARCH.md §Standard Stack). Zero registry vetting required.

---

## Pre-Populated From

| Source | Decisions Used |
|--------|---------------|
| `01-CONTEXT.md` D-01..D-17 | 17 — all decisions pre-populated, zero re-asked |
| `01-RESEARCH.md` | Component patterns, skill categories, language levels, date format utility |
| `01-PATTERNS.md` | Exact Tailwind class conventions, file structure, analog map |
| `01-03-PLAN.md` | Editor acceptance criteria, XSS prohibitions, copy button behavior |
| `01-04-PLAN.md` | SkillChips pattern, NormGapPanel pattern, language dropdown |
| `scanready/src/app/page.tsx` (walking skeleton) | All existing class names, copy strings, state machine, LockIcon SVG |
| `scanready/src/app/globals.css` | Tailwind v4 `@import`, CSS variables, font family |
| `scanready/src/app/layout.tsx` | Font variable names, html/body class structure |
| User input | 0 — zero questions asked; all decisions were already locked upstream |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 01-pre-flight-parse-flow*
*UI-SPEC created: 2026-06-22*
*Serves: Plans 01-03 (WYSIWYG editor core) and 01-04 (skill chips + norm-gap panel)*
