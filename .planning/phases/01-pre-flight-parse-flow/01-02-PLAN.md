---
phase: 01-pre-flight-parse-flow
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scanready/src/lib/schema.ts
  - scanready/src/lib/prompts.ts
autonomous: true
requirements: [LL-01, LL-02]
must_haves:
  truths:
    - "The parse output groups skills into German-conventional categories instead of a flat string list"
    - "Language levels in the parse output use German CV vocabulary, not CEFR codes or 'native'/'fluent'"
    - "normGapNotes read as concise one-line change + why-it-matters statements"
    - "Grounding (no fabrication) is preserved in every PARSE_SYSTEM addition"
    - "A live /api/parse round-trip returns a schema-valid Lebenslauf with categorized skills"
  artifacts:
    - path: "scanready/src/lib/schema.ts"
      provides: "SkillCategorySchema and categorized skills field"
      contains: "SkillCategorySchema"
      exports: ["SkillCategorySchema", "SkillCategory", "LebenslaufSchema", "Lebenslauf"]
    - path: "scanready/src/lib/prompts.ts"
      provides: "PARSE_SYSTEM with D-07 note style, D-09 categories, D-10 language levels"
      contains: "IT-Kenntnisse"
  key_links:
    - from: "scanready/src/lib/schema.ts"
      to: "scanready/src/app/api/parse/route.ts"
      via: "LebenslaufSchema consumed by zodOutputFormat in the parse route"
      pattern: "zodOutputFormat\\(LebenslaufSchema\\)"
    - from: "scanready/src/lib/prompts.ts"
      to: "scanready/src/app/api/parse/route.ts"
      via: "PARSE_SYSTEM passed as the system prompt"
      pattern: "PARSE_SYSTEM"
---

<objective>
Upgrade the parse backend so the structured output matches the German-norm editor coming in Plans 03-04: categorized skills (D-09), German-vocabulary language levels (D-10), and a concise "change + why it matters" voice for norm-gap notes (D-07). This is a grounding-safe schema + prompt change shipped in a SINGLE plan/commit because the two files are coupled (schema expects the new shape; the prompt must instruct the model to produce it).

Covers the backend half of LL-01 (norm-correct structure incl. categorized Kenntnisse + German language levels) and LL-02 (the concise norm-gap note style rendered later from `normGapNotes[]`).

Purpose: Without this, the editor (Plan 03/04) would render a flat skill list and CEFR/"native" language strings — exactly the expat norm gaps the tool exists to fix.
Output: `schema.ts` gains `SkillCategorySchema` + `SkillCategory` and switches `skills` to `z.array(SkillCategorySchema)`; `prompts.ts` `PARSE_SYSTEM` gains D-07/D-09/D-10 instructions with grounding reinforced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-flight-parse-flow/01-CONTEXT.md
@.planning/phases/01-pre-flight-parse-flow/01-RESEARCH.md
@.planning/phases/01-pre-flight-parse-flow/01-PATTERNS.md
@scanready/.claude/CLAUDE.md
@scanready/src/lib/schema.ts
@scanready/src/lib/prompts.ts
@scanready/src/app/api/parse/route.ts
</context>

## Phase Goal

**As a** an expat job-seeker, **I want to** paste my US/UK résumé and get an editable, norm-correct German Lebenslauf with bilingual "what changed & why" notes, **so that** I can submit a CV a German recruiter takes seriously in the 8-second scan.

(Plan 02 delivers the backend grounding-safe upgrade that makes the categorized-Kenntnisse, German-language-level, and concise-note parts of that story possible.)

## Artifacts this phase produces (Plan 02 portion)

| Symbol / artifact | File | Kind |
|---|---|---|
| `SkillCategorySchema` (`{ category: string, skills: string[] }`) | `scanready/src/lib/schema.ts` | new Zod schema (skill-category type) |
| `SkillCategory` type (`z.infer`) | `scanready/src/lib/schema.ts` | new exported type |
| `skills` field changed to `z.array(SkillCategorySchema)` | `scanready/src/lib/schema.ts` | schema edit |
| `PARSE_SYSTEM` D-07 note-style + D-09 categories + D-10 language-level instructions | `scanready/src/lib/prompts.ts` | prompt edit |

<tasks>

<task type="auto">
  <name>Task 1: Categorized skills schema — add SkillCategorySchema, switch skills field, export SkillCategory (D-09)</name>
  <files>scanready/src/lib/schema.ts</files>
  <read_first>
    - scanready/src/lib/schema.ts (full current file — the `skills: z.array(z.string())` line at ~line 38 is the only field that changes; everything else stays)
    - .planning/phases/01-pre-flight-parse-flow/01-PATTERNS.md §"scanready/src/lib/schema.ts" (exact insertion: `SkillCategorySchema` before `LebenslaufSchema`, the replaced `skills` line, the `SkillCategory` type export after `LebenslaufSchema`) and §"Zod v4 safety rules"
    - .planning/phases/01-pre-flight-parse-flow/01-RESEARCH.md §"German Skill Categories (D-09)", §"Schema/Prompt Change Safety" (Zod v4 checklist), and Pitfall 5 (no `.min()`/`.max()` on numbers)
    - scanready/.claude/CLAUDE.md "Zod v4 — standard primitives only" constraint
  </read_first>
  <action>
    In `scanready/src/lib/schema.ts`, define `SkillCategorySchema` as a `z.object` with two required fields: `category` (a `z.string()` — holds labels such as IT-Kenntnisse, Fachkenntnisse, Sonstige Kenntnisse) and `skills` (a `z.array(z.string())`). Place it before `LebenslaufSchema`. Then change the existing `skills` field inside `LebenslaufSchema` from a flat string array to `z.array(SkillCategorySchema)`. Export the inferred type `SkillCategory` (via `z.infer<typeof SkillCategorySchema>`) after `LebenslaufSchema`, alongside the existing `Lebenslauf` type export.

    Zod v4 / Anthropic structured-output safety (mandatory): use only `z.string`, `z.array`, `z.object`, and `.nullable` where already present. Add no numeric range validators of any kind, no discriminated union, no recursive refs, and keep both `category` and `skills` required (not optional). The root must remain a `z.object`. Do not touch any other field in `LebenslaufSchema` — `personal`, `experience`, `education`, `languages`, `normGapNotes`, `photoAdvice` are unchanged.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && grep -q "SkillCategorySchema" src/lib/schema.ts && grep -q "skills: z.array(SkillCategorySchema)" src/lib/schema.ts && grep -q "export type SkillCategory" src/lib/schema.ts && ! grep -E "\.(min|max)\(" src/lib/schema.ts && ! grep -q "discriminatedUnion" src/lib/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `schema.ts` declares `SkillCategorySchema` as a `z.object` with `category: z.string()` and `skills: z.array(z.string())`
    - `LebenslaufSchema.skills` is `z.array(SkillCategorySchema)` (no longer a flat string array)
    - `schema.ts` exports both `SkillCategory` and the existing `Lebenslauf` type
    - No numeric `.min(`/`.max(` validators and no `discriminatedUnion` anywhere in `schema.ts` (Anthropic structured-output safety)
    - `personal`, `experience`, `education`, `languages`, `normGapNotes`, `photoAdvice` are unchanged
  </acceptance_criteria>
  <done>schema.ts produces a categorized skills shape that is Zod-v4 / Anthropic-structured-output safe and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: PARSE_SYSTEM upgrade — note style (D-07), skill categories (D-09), German language levels (D-10), grounding preserved</name>
  <files>scanready/src/lib/prompts.ts</files>
  <read_first>
    - scanready/src/lib/prompts.ts (full current file — `PARSE_SYSTEM` is the only constant edited; `COVER_LETTER_SYSTEM`, `buildParseUser`, `buildCoverLetterUser`, `PERSONALIZATION_QUESTIONS` are untouched)
    - .planning/phases/01-pre-flight-parse-flow/01-PATTERNS.md §"scanready/src/lib/prompts.ts" (the exact D-07 replacement of the normGapNotes line, and the D-09 + D-10 additions to the GERMAN NORMS block) and §"Grounding Rule (Prompts)"
    - .planning/phases/01-pre-flight-parse-flow/01-RESEARCH.md §"German Skill Categories (D-09)" (PARSE_SYSTEM addition), §"Language Level Dropdown (D-10)" (PARSE_SYSTEM mapping guide), §"Schema/Prompt Change Safety" (D-07 note-style block), Pitfall 2 (schema+prompt must change together)
    - scanready/src/lib/schema.ts (now categorized after Task 1 — the prompt must instruct the model to fill the new shape)
  </read_first>
  <action>
    Edit only the `PARSE_SYSTEM` constant in `scanready/src/lib/prompts.ts`. Make three changes, all inside the GERMAN NORMS area, leaving the top GROUNDING block intact:

    D-07 (note voice): Replace the current single `normGapNotes` instruction line with an instruction that, for each structural or content change made, the model writes ONE English line in the form "[change made] — [why it matters in the 8-second German recruiter scan]", with a concrete example (e.g. an added personal-data block and why recruiters scan for it), tells it to be concise and not repeat content already visible in the Lebenslauf, and reaffirms grounding (notes describe changes actually made, not improvements that were not possible).

    D-09 (skill categories): Add an instruction to group skills into German-conventional categories — IT-Kenntnisse for software/programming/technical tools, Fachkenntnisse for domain/industry-specific knowledge, Sonstige Kenntnisse for certifications/driver's licenses/other qualifications — to include only categories that have entries drawn from the source CV, to not invent skills, and to not list soft skills as standalone entries (omit them or weave them into experience bullets). The literal category labels IT-Kenntnisse, Fachkenntnisse, and Sonstige Kenntnisse must appear in the prompt text.

    D-10 (language levels): Add an instruction to map the applicant's stated proficiency onto German CV vocabulary using exactly one of Muttersprache / Verhandlungssicher / Fließend / Gute Kenntnisse / Grundkenntnisse, to avoid CEFR codes and "native"/"fluent"/free-form text, with a short mapping guide (native -> Muttersprache; business-fluent/C1-C2 -> Verhandlungssicher; fluent/conversational/B2 -> Fließend; intermediate/B1 -> Gute Kenntnisse; basic/A1-A2 -> Grundkenntnisse), returning null when the level is absent.

    Each addition that touches output fields must reaffirm grounding (an explicit "do not invent" / "only include ... from the source CV" clause), per the project grounding rule. Do not weaken the existing GROUNDING block. Do not edit `COVER_LETTER_SYSTEM` or any other export.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && grep -q "IT-Kenntnisse" src/lib/prompts.ts && grep -q "Fachkenntnisse" src/lib/prompts.ts && grep -q "Sonstige Kenntnisse" src/lib/prompts.ts && grep -q "Muttersprache" src/lib/prompts.ts && grep -q "Verhandlungssicher" src/lib/prompts.ts && grep -q "Grundkenntnisse" src/lib/prompts.ts && grep -q "GROUNDING" src/lib/prompts.ts</automated>
    <human-check>Run a live `/api/parse` round-trip with a sample CV (via `npm run dev` + the page, or curl): confirm the returned JSON has `skills` as an array of `{ category, skills }` with German category labels, `languages[].level` using the German vocabulary, and `normGapNotes` reading as concise one-line change+why statements.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `PARSE_SYSTEM` contains the literal labels IT-Kenntnisse, Fachkenntnisse, Sonstige Kenntnisse (D-09)
    - `PARSE_SYSTEM` contains the German language-level vocabulary Muttersprache / Verhandlungssicher / Fließend / Gute Kenntnisse / Grundkenntnisse (D-10)
    - `PARSE_SYSTEM` instructs the concise "[change] — [why it matters]" one-line note voice (D-07)
    - The top GROUNDING block remains intact and each new field-touching instruction reaffirms grounding ("do not invent" / "only include ... from the source CV")
    - `COVER_LETTER_SYSTEM`, `buildParseUser`, `buildCoverLetterUser`, `PERSONALIZATION_QUESTIONS` are unchanged
    - A live `/api/parse` round-trip returns a schema-valid `Lebenslauf` with categorized skills (no Zod validation failure / null `parsed_output`)
  </acceptance_criteria>
  <done>PARSE_SYSTEM instructs categorized German skills, German language levels, and concise norm-gap notes while preserving grounding; a live parse returns the new categorized shape and validates against the updated schema.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Block on: high severity. This plan edits server-side schema + prompt only; no new endpoints, no client surface, no persistence.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `/api/parse` | Untrusted pasted CV text reaches the (now-edited) `PARSE_SYSTEM` prompt |
| `PARSE_SYSTEM` -> model output | The prompt shapes what the model emits; weakened grounding could let fabricated content through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Prompt edits weakening the no-fabrication grounding (e.g. inventing skills/categories/levels) | mitigate (high) | Each D-07/D-09/D-10 addition reaffirms "do not invent" / "only include from the source CV"; the top GROUNDING block is untouched. Verified by grep for GROUNDING + acceptance review of grounding clauses. |
| T-01-06 | Tampering | Schema change emitting unsupported JSON Schema keywords -> Anthropic 400 / null `parsed_output` | mitigate | Zod-v4 safe shape only (no `.min`/`.max`, no discriminatedUnion); verified by negative grep + live round-trip returning a valid object. |
| T-01-07 | Tampering | Prompt injection via CV text steering categorization | accept (already-mitigated) | `zodOutputFormat(LebenslaufSchema)` constrains output shape; the model cannot emit arbitrary JSON regardless of injected instructions. |
| T-01-SC | Tampering | dependency installs | accept | No package-manager changes in this plan (no new deps). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `schema.ts`: `SkillCategorySchema` present; `skills` is `z.array(SkillCategorySchema)`; `SkillCategory` exported; no `.min`/`.max`/`discriminatedUnion`.
- `prompts.ts`: D-07 note voice + D-09 category labels + D-10 language vocabulary present; GROUNDING intact.
- Live `/api/parse` returns a schema-valid `Lebenslauf` with categorized skills + German language levels.
</verification>

<success_criteria>
The parse backend produces a grounded, schema-valid German Lebenslauf with skills grouped into German-conventional categories, language levels in German CV vocabulary, and concise change+why norm-gap notes — the backend foundation for the LL-01/LL-02 editor and panel in Plans 03-04 — with no regression to the no-fabrication grounding.
</success_criteria>

<output>
Create `.planning/phases/01-pre-flight-parse-flow/01-02-SUMMARY.md` when done.
</output>
