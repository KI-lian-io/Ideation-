---
phase: 07-account-library-honest-pricing-design-reconciliation-phase-a
plan: 08
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - scanready/src/app/preise/page.tsx
autonomous: true
requirements: [SC-07]
must_haves:
  truths:
    - "/preise shows a four-card ladder: Humanizer+ 2,99 / Bewerbungspaket 4,99 / Bewerbungsphase-Pass 14,99 (featured, Empfohlen, per-application math) / ScanReady Plus 5,99 (Geplant, non-buyable)"
    - "The free-tier manifesto pull quote is present"
    - "The anti-Abofalle before/after comparison is present with no competitor names"
    - "Displayed prices are formatted from the code constants, not hardcoded strings"
  artifacts:
    - path: "scanready/src/app/preise/page.tsx"
      provides: "Rebuilt four-card pricing ladder + manifesto + anti-Abofalle comparison"
      contains: "PASS_PRICE_CENTS"
  key_links:
    - from: "scanready/src/app/preise/page.tsx"
      to: "scanready/src/lib/humanizer.ts"
      via: "imports HUMANIZER_PRICE_CENTS, PAKET_PRICE_CENTS, PASS_PRICE_CENTS and formats them"
      pattern: "PASS_PRICE_CENTS"
---

<objective>
A5 Preise v2 (design surface 04). Full rebuild of `src/app/preise/page.tsx` into the four-card honest-pricing ladder (Humanizer+, Bewerbungspaket, featured Bewerbungsphase-Pass with per-application math, and a "Geplant" non-buyable ScanReady Plus), the free-tier manifesto pull quote, and the anti-Abofalle before/after comparison - with every price formatted from the code constants.

Purpose: Make the pricing page state the honest positioning out loud and show the Pass as the volume-friendly, non-renewing option, before any paywall moment.
Output: rebuilt preise/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
## Phase Goal

**As a** prospective buyer comparing options, **I want to** see ScanReady's full pricing ladder with the Pass and its per-application math laid out honestly, **so that** I trust the offer and can pick the volume-friendly, non-renewing option before any paywall.

(Derived from PRD sections 4 and 6.2; the ROADMAP phase goal is outcome-shaped.)

Depends on 07-02 (PASS_PRICE_CENTS). Preise is a German-only static server component (no 'use client', no i18n toggle) - keep that shape. Independent of the tool UI plans, so it runs in Wave 2 alongside the Pass API layer.

@.planning/phases/07-account-library-honest-pricing-design-reconciliation-phase-a/07-CONTEXT.md
@.planning/phases/07-account-library-honest-pricing-design-reconciliation-phase-a/07-PATTERNS.md
@scanready/src/app/preise/page.tsx
@scanready/src/lib/humanizer.ts
@scanready/src/components/ui.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the four-card pricing ladder (prices from constants)</name>
  <files>scanready/src/app/preise/page.tsx</files>
  <read_first>
    - scanready/claude design files/Scanready award-winning design_updated/04-preise.dc.html (the full DE deck + design notes: H1 "Bezahlen Sie die Bewerbungsphase. Nicht jede Bewerbung.", the four cards, the featured Pass card with glow + "Empfohlen" + per-application math "Bei 10 Bewerbungen: 1,50 EUR pro Bewerbung", the Plus "Geplant" card with grey checks + "Benachrichtigen lassen")
    - scanready/src/app/preise/page.tsx (current structure: server component, `export const metadata`, the `lang="de"` root wrapper, the masthead/nav, CARD/EYEBROW/Btn imports, FolioEyebrow/CheckIcon helpers - keep these shapes)
    - scanready/src/lib/humanizer.ts (HUMANIZER_PRICE_CENTS=299, PAKET_PRICE_CENTS=499, PASS_PRICE_CENTS=1499)
  </read_first>
  <action>
    Rebuild preise/page.tsx keeping it a plain server component with the existing metadata const and `lang="de"` wrapper. Replace the current 3-card structure with the four-card ladder from surface 04, copy VERBATIM from the deck: Humanizer+ (2,99), Bewerbungspaket (4,99), Bewerbungsphase-Pass (14,99, "einmalig middot 30 Tage", featured with the green brand-glow, "Empfohlen", and the per-application math line), and ScanReady Plus (5,99/Monat, "Geplant" chip, grey checks, a non-buyable "Benachrichtigen lassen" CTA rendered as a plain link/Btn, no client handler). CRITICAL: import HUMANIZER_PRICE_CENTS / PAKET_PRICE_CENTS / PASS_PRICE_CENTS and format each displayed price from the constant (a small cents-to-EUR de-DE formatter), so prices match the code constants structurally rather than by eyeballed literals. No em-dash characters (use middot for "einmalig middot 30 Tage").
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && grep -c "PASS_PRICE_CENTS\|PAKET_PRICE_CENTS\|HUMANIZER_PRICE_CENTS" src/app/preise/page.tsx && grep -c "Geplant" src/app/preise/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "PASS_PRICE_CENTS|PAKET_PRICE_CENTS|HUMANIZER_PRICE_CENTS" scanready/src/app/preise/page.tsx` is at least 3 (all three prices formatted from constants)
    - `grep -c "Geplant" scanready/src/app/preise/page.tsx` is at least 1 and the Plus CTA is non-buyable ("Benachrichtigen lassen", not a checkout trigger)
    - `grep -c "use client" scanready/src/app/preise/page.tsx` is 0 (still a server component)
    - The per-application math line is present (grep for "pro Bewerbung")
    - `cd scanready && npx tsc --noEmit` exits 0
    - `grep -rnP "\x{2014}" scanready/src/app/preise/page.tsx` returns no matches
  </acceptance_criteria>
  <done>Preise renders the four-card ladder with the featured non-renewing Pass, the Plus "Geplant" non-buyable card, and every price formatted from the code constants.</done>
</task>

<task type="auto">
  <name>Task 2: Free-tier manifesto pull quote + anti-Abofalle comparison</name>
  <files>scanready/src/app/preise/page.tsx</files>
  <read_first>
    - scanready/claude design files/Scanready award-winning design_updated/04-preise.dc.html (the free-tier manifesto pull-quote band and the "Andere CV-Dienste" terracotta vs "ScanReady" accent before/after comparison; the design annotation that competitor names must not appear)
    - scanready/src/app/preise/page.tsx (Task 1 rebuild; the PullQuote-style band pattern already used on the landing / in ds-update editorial components)
    - scanready/src/app/globals.css (the brand-error terracotta token for the "Andere CV-Dienste" column)
  </read_first>
  <action>
    Add the free-tier manifesto pull-quote band with the exact copy "Kostenlos heisst vollstaendig. Wir begrenzen, wie viel wir fuer Sie speichern, nie wie gut Ihr Dokument ist." (correct umlauts in the file). Add the anti-Abofalle before/after comparison: an "Andere CV-Dienste" column in the terracotta brand-error tone versus a "ScanReady" column in the accent tone, contrasting the trial-trap pattern with the honest one-time/non-renewing model. NO competitor names appear in the UI (generic "Andere CV-Dienste" only). No em-dash characters.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && npm run build && grep -c "Kostenlos hei" src/app/preise/page.tsx && grep -c "Andere CV-Dienste" src/app/preise/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The manifesto pull quote "Kostenlos heisst vollstaendig..." is present (grep matches the leading phrase)
    - `grep -c "Andere CV-Dienste" scanready/src/app/preise/page.tsx` is at least 1 and the before/after comparison renders two contrasting columns
    - No competitor brand names appear: `grep -ciE "cvapp|zety|enhancv|kickresume|rezi|teal|lebenslauf\.de" scanready/src/app/preise/page.tsx` is 0
    - `cd scanready && npx tsc --noEmit && npm run build` both exit 0
    - `grep -rnP "\x{2014}" scanready/src/app/preise/page.tsx` returns no matches
  </acceptance_criteria>
  <done>Preise includes the free-tier manifesto pull quote and the competitor-name-free anti-Abofalle before/after comparison, and the page builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content -> visitor | Static server-rendered pricing page; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | price display drift | mitigate | Prices are formatted from the code constants (PASS/PAKET/HUMANIZER_PRICE_CENTS), so the page cannot silently diverge from what is charged |
| T-07-08-02 | Repudiation | misleading pricing claims | mitigate | The non-renewing Pass and "Geplant" Plus are stated honestly; PAngV total-price display; the manifesto states the free-tier limit is storage-only |
| T-07-08-03 | Information disclosure | static page | accept | No user data, no I/O, no DB on this route |
</threat_model>

<artifacts_this_phase_produces>
NEW symbols this plan creates:
- Rebuilt `src/app/preise/page.tsx` (four-card ladder, manifesto pull quote, anti-Abofalle comparison); any new local presentational helpers added within that file
</artifacts_this_phase_produces>

<verification>
- `cd scanready && npx tsc --noEmit` exits 0
- `cd scanready && npm test` exits 0
- `cd scanready && npm run build` exits 0
- `grep -rnP "\x{2014}" scanready/src/app/preise/page.tsx` returns no matches
</verification>

<success_criteria>
Satisfies ROADMAP SC-07: /preise shows the 4-card ladder (2,99 / 4,99 / 14,99 featured with per-application math / Plus 5,99 "Geplant" non-buyable), the free-tier manifesto pull quote, and the anti-Abofalle before/after comparison; prices match the code constants.
</success_criteria>

<output>
Create `.planning/phases/07-account-library-honest-pricing-design-reconciliation-phase-a/07-08-SUMMARY.md` when done.
</output>
