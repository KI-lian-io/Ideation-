---
phase: 07-account-library-honest-pricing-design-reconciliation-phase-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scanready/src/app/globals.css
  - scanready/src/components/ui.tsx
  - scanready/src/app/layout.tsx
  - scanready/src/app/not-found.tsx
  - scanready/public/favicon.svg
  - scanready/public/og-scanready.svg
  - scanready/public/scanready-lockup.svg
  - scanready/public/scanready-lockup-dark.svg
  - scanready/public/scanready-mark.svg
  - scanready/public/scanready-stacked.svg
  - scanready/public/favicon.ico
autonomous: true
requirements: [SC-08]
must_haves:
  truths:
    - "The generated shadow utilities shadow-sheet / shadow-sheet-hero / shadow-modal exist and compile in a Tailwind v4 class"
    - "src/components/ui.tsx exports SheetCard, KebabMenu, InlineRenameField, MonoBadge, EmptyState, BottomSheet, SavedConfirmationPanel"
    - "The six founder SVGs are in public/ and the app favicon + Open Graph image resolve to them"
    - "A German-first /not-found route renders the misprint-correction notice with no animation"
    - "npx tsc --noEmit and npm run build pass"
  artifacts:
    - path: "scanready/src/components/ui.tsx"
      provides: "A0 primitives (SheetCard, KebabMenu, InlineRenameField, MonoBadge, EmptyState, BottomSheet, SavedConfirmationPanel)"
      contains: "export function KebabMenu"
    - path: "scanready/src/app/globals.css"
      provides: "shadow-sheet / shadow-sheet-hero / shadow-modal @theme tokens"
      contains: "--shadow-sheet"
    - path: "scanready/src/app/not-found.tsx"
      provides: "Surface-06 misprint-correction 404 page"
      min_lines: 20
    - path: "scanready/src/app/layout.tsx"
      provides: "icons + openGraph.images metadata wiring"
      contains: "openGraph"
  key_links:
    - from: "scanready/src/app/layout.tsx"
      to: "scanready/public/favicon.svg"
      via: "metadata.icons references /favicon.svg"
      pattern: "favicon\\.svg"
    - from: "scanready/src/app/layout.tsx"
      to: "scanready/public/og-scanready.svg"
      via: "metadata.openGraph.images references /og-scanready.svg"
      pattern: "og-scanready\\.svg"
---

<objective>
A0 Foundation (locked build-order wave, unblocks every later slice). Add the sheet/modal shadow tokens, the seven shared UI primitives the library + pass + save surfaces are built from, swap in the six founder brand assets, wire favicon + Open Graph metadata, and ship a branded /not-found page from design surface 06.

Purpose: Every later plan (07-04..07-08) consumes these primitives and tokens. Shipping them first, alone, keeps each feature-slice plan inside its context budget and prevents primitive drift across surfaces.
Output: Extended `ui.tsx`, `globals.css` shadow tokens, `public/` asset swap, `layout.tsx` metadata, new `not-found.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
This plan is the authorized A0 foundation wave (per 07-CONTEXT.md locked build order A0 -> A1 -> A2 -> A3 -> A4 -> A5). It carries no user-facing slice on its own; it is the primitive layer for the phase user story: "As a signed-in ScanReady user applying to many jobs in Germany, I want to save, browse, rename and duplicate my application packages and pay once for my whole Bewerbungsphase, so that I reuse my work in seconds and my per-application cost stays honest." (Derived from PRD sections 2 and 5; the ROADMAP goal is outcome-shaped, not user-story-shaped.)

@.planning/phases/07-account-library-honest-pricing-design-reconciliation-phase-a/07-CONTEXT.md
@.planning/phases/07-account-library-honest-pricing-design-reconciliation-phase-a/07-PATTERNS.md
@scanready/docs/design-impl-plan-library-pricing.md
@scanready/src/components/ui.tsx
@scanready/src/app/globals.css
@scanready/src/app/layout.tsx
@scanready/DESIGN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shadow tokens + seven ui.tsx primitives</name>
  <files>scanready/src/app/globals.css, scanready/src/components/ui.tsx</files>
  <read_first>
    - scanready/src/components/ui.tsx (current exports: btnClass, Btn, CARD, EYEBROW, SKILL_CHIP, NORM_NOTE; const-vs-component doctrine in the header comment)
    - scanready/src/app/globals.css lines 5-92 (the @theme block and the existing hardcoded .doc-sheet shadow)
    - scanready/src/components/PaketModal.tsx (the Escape+Tab focus-trap useEffect and focus-management useEffect - copy the trap logic for KebabMenu and BottomSheet)
    - scanready/claude design files/Scanready award-winning design_updated/ds-update/tokens/shadows.css (exact --shadow-sheet / --shadow-sheet-hero / --shadow-modal values)
    - scanready/claude design files/Scanready award-winning design_updated/ds-update/components/surfaces/ and .../editorial/ prompt.md + .card.html files (primitive shape/behavior spec: SheetCard, badges, empty state)
    - scanready/claude design files/Scanready award-winning design_updated/01-library.dc.html (design-note annotations for SheetCard / KebabMenu / InlineRenameField / MonoBadge / EmptyState anatomy)
  </read_first>
  <action>
    In globals.css `@theme`, add three tokens copied from ds-update/tokens/shadows.css: `--shadow-sheet`, `--shadow-sheet-hero`, `--shadow-modal` (keep the exact multi-layer rgba values). In Tailwind v4 these generate the `shadow-sheet`, `shadow-sheet-hero`, `shadow-modal` utilities. Do not delete or edit the existing `.doc-sheet` rule.

    In src/components/ui.tsx add seven primitives, honoring the file's const-vs-component doctrine (plain string const when there is no state/handler; function component only when internal state or events are needed):
    - `SheetCard` - const class string: sheet shadow (`shadow-sheet`), `border border-hair`, 12px radius, `bg-card`. The doc-sheet card shell used by library cards.
    - `MonoBadge` - render badges as EYEBROW-style mono chips. Provide the LL / AS / STELLENANZEIGE present-variant, a dashed ghost "fehlt" variant (never red - use hairline/muted, not brand-error), and two pill variants: `BEARBEITBAR` (accent-tint) and `NUR LESEN` (slate text plus a lock glyph). A discriminated `variant` prop is fine; the label text is passed in by the caller (copy comes from i18n at the call site, not hardcoded here).
    - `EmptyState` - component: dashed border, a mono status line that is ALSO the aria-live region (`role="status"` / `aria-live="polite"`), a body line, and exactly one CTA rendered with `Btn`/`btnClass`. All text via props.
    - `SavedConfirmationPanel` - component: accent-tint panel with a checkmark, a timestamp slot, an editable-title slot, and an "open in library" link rendered with `Btn`/`btnClass`. All text via props.
    - `KebabMenu` - component: icon trigger plus a floating menu; a hairline separator before the destructive item; Escape closes, outside-click closes, and Tab is focus-trapped inside the open menu. Copy the trap effect from PaketModal.tsx scoped to the menu ref. Menu items are passed as props (label + onSelect + optional `destructive` flag).
    - `InlineRenameField` - component: serif input (`font-serif-text`), a 2px accent focus ring, Enter commits (calls an `onSave` prop), Escape cancels (calls `onCancel`). Controlled value via props.
    - `BottomSheet` - component: mobile replacement for KebabMenu with 44px minimum touch targets; same Escape/outside-click/focus-trap a11y. Build from the ds-update spec plus the KebabMenu trap logic (no existing analog).

    No em-dash characters anywhere in new code or comments (site-wide ban). Do not add i18n keys here - primitives receive their strings from callers.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\-\-shadow-sheet' scanready/src/app/globals.css` is at least 1 and `--shadow-modal` and `--shadow-sheet-hero` are both present in the @theme block
    - `grep -E 'export (function|const) (SheetCard|KebabMenu|InlineRenameField|MonoBadge|EmptyState|BottomSheet|SavedConfirmationPanel)' scanready/src/components/ui.tsx` matches all seven names
    - `grep -c 'onKeyDown\|Escape\|keydown' scanready/src/components/ui.tsx` is at least 1 (KebabMenu/BottomSheet keyboard handling present)
    - `cd scanready && npx tsc --noEmit` exits 0
    - `grep -rnP "\x{2014}" scanready/src/components/ui.tsx scanready/src/app/globals.css` returns no matches (em-dash ban)
  </acceptance_criteria>
  <done>The three shadow utilities exist as @theme tokens, ui.tsx exports all seven primitives with the required a11y behaviors, and typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Founder asset swap + favicon + Open Graph metadata</name>
  <files>scanready/public/favicon.svg, scanready/public/og-scanready.svg, scanready/public/scanready-lockup.svg, scanready/public/scanready-lockup-dark.svg, scanready/public/scanready-mark.svg, scanready/public/scanready-stacked.svg, scanready/public/favicon.ico, scanready/src/app/layout.tsx</files>
  <read_first>
    - scanready/src/app/layout.tsx (the static `metadata: Metadata` const at lines ~13-30; keep it a single const, do not add generateMetadata)
    - scanready/claude design files/Scanready award-winning design_updated/06-founder-assets.dc.html (asset intent + the stand-in-mark caveat)
    - scanready/claude design files/Scanready award-winning design_updated/assets/ (the six source SVGs)
    - scanready/public/ (current og-image.svg to be replaced)
  </read_first>
  <action>
    Copy the six SVGs from the export `assets/` folder into `scanready/public/`: `favicon.svg`, `og-scanready.svg` (this REPLACES the off-brand `public/og-image.svg`; remove the old file or overwrite the reference), `scanready-lockup.svg`, `scanready-lockup-dark.svg`, `scanready-mark.svg`, `scanready-stacked.svg`. Regenerate `public/favicon.ico` from `favicon.svg` (any local rasterizer is fine; a 32x32 ICO is sufficient).

    In layout.tsx, extend the existing `metadata` const: add an `icons` key pointing at `/favicon.svg` (and the `.ico` shortcut) and add an explicit `openGraph.images` array referencing `/og-scanready.svg` with width/height/alt. Do not create a second metadata export. Add a short code comment noting the marks are stand-ins pending the naming decision (naming-shortlist.md), matching the design's stand-in caveat.
  </action>
  <verify>
    <automated>cd scanready && ls public/favicon.svg public/og-scanready.svg public/scanready-lockup.svg public/scanready-lockup-dark.svg public/scanready-mark.svg public/scanready-stacked.svg && grep -q "og-scanready.svg" src/app/layout.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All six SVGs exist under scanready/public/ (ls exits 0)
    - `grep -c "favicon.svg" scanready/src/app/layout.tsx` is at least 1 and `grep -c "og-scanready.svg" scanready/src/app/layout.tsx` is at least 1
    - `grep -c "openGraph" scanready/src/app/layout.tsx` is at least 1 and includes an `images` array
    - The stale `public/og-image.svg` is no longer referenced anywhere: `grep -rn "og-image.svg" scanready/src` returns no matches
  </acceptance_criteria>
  <done>The six founder assets are in public/, favicon.ico is regenerated, and layout.tsx metadata resolves icons + openGraph.images to the new assets with no lingering og-image.svg reference.</done>
</task>

<task type="auto">
  <name>Task 3: Branded /not-found page from surface 06</name>
  <files>scanready/src/app/not-found.tsx</files>
  <read_first>
    - scanready/claude design files/Scanready award-winning design_updated/06-founder-assets.dc.html (the "misprint correction notice" 404 copy deck, DE and EN, plus its reduced-motion / no-animation note)
    - scanready/src/app/preise/page.tsx lines ~44-60 (the sticky masthead/nav wordmark block to reuse as page chrome; note the `lang="de"` root wrapper convention and that it is a plain server component)
    - scanready/src/app/layout.tsx (to avoid duplicating chrome the layout already provides)
    - .planning/phases/07-account-library-honest-pricing-design-reconciliation-phase-a/07-PATTERNS.md (the not-found.tsx guidance: follow the split-static-page precedent, not the useLang toggle)
  </read_first>
  <action>
    Create `scanready/src/app/not-found.tsx` as a plain server component (no `'use client'`). Lift the surface-06 "misprint correction notice" copy VERBATIM. Follow the /de-vs-/ split-static-page precedent from PATTERNS.md: render German copy as the primary text with the English line as a secondary caption in the same page (do NOT wrap in LangProvider / useLang, since not-found renders outside the tool's LangProvider). Reuse the preise masthead/nav wordmark for chrome and provide a link back to `/`. Zero animation: do not import any Typesetting Theater scan-sweep or motion classes; the page must be reduced-motion-safe by containing no motion at all.
  </action>
  <verify>
    <automated>cd scanready && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `scanready/src/app/not-found.tsx` exists and default-exports a component
    - `grep -c "use client" scanready/src/app/not-found.tsx` is 0 (server component)
    - `grep -c "scan-sweep\|animate-\|motion-" scanready/src/app/not-found.tsx` is 0 (no animation)
    - `cd scanready && npm run build` exits 0 and the build output lists a `/not-found` (or `/_not-found`) route
    - `grep -rnP "\x{2014}" scanready/src/app/not-found.tsx` returns no matches
  </acceptance_criteria>
  <done>Visiting an unknown route renders the branded misprint-correction 404 with no animation, and both tsc and next build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time asset -> public/ | Static SVGs served to any visitor; no user input crosses here |
| unknown-route -> not-found.tsx | Any URL path reaches the 404 renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | public/ SVG assets | accept | Static brand assets, no user data, served read-only from public/ |
| T-07-01-02 | Information disclosure | not-found.tsx | mitigate | Server component renders static copy only; no request data, params, or stack traces echoed into the page |
| T-07-01-03 | Denial of service | not-found.tsx | accept | Static render, no I/O, no DB; unknown-route flood is handled by the platform, not app code |
</threat_model>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols/files (exclude from source-drift verification):
- Components/exports in `src/components/ui.tsx`: `SheetCard`, `KebabMenu`, `InlineRenameField`, `MonoBadge`, `EmptyState`, `BottomSheet`, `SavedConfirmationPanel`
- CSS tokens/utilities: `--shadow-sheet`, `--shadow-sheet-hero`, `--shadow-modal` (and the Tailwind `shadow-sheet` / `shadow-sheet-hero` / `shadow-modal` utilities)
- Route: `src/app/not-found.tsx`
- Assets: `public/favicon.svg`, `public/og-scanready.svg`, `public/scanready-lockup.svg`, `public/scanready-lockup-dark.svg`, `public/scanready-mark.svg`, `public/scanready-stacked.svg`, regenerated `public/favicon.ico`
- Metadata keys added to `src/app/layout.tsx`: `metadata.icons`, `metadata.openGraph.images`
</artifacts_this_phase_produces>

<verification>
- `cd scanready && npx tsc --noEmit` exits 0
- `cd scanready && npm test` exits 0 (existing suite still green; this plan adds no tests but must not regress)
- `cd scanready && npm run build` exits 0
- `grep -rnP "\x{2014}" scanready/src/components/ui.tsx scanready/src/app/globals.css scanready/src/app/not-found.tsx scanready/src/app/layout.tsx` returns no matches
</verification>

<success_criteria>
Contributes to ROADMAP SC-08: founder SVGs in public/, favicon + openGraph.images wired in layout.tsx, not-found.tsx built from surface 06, tsc + build pass, no em-dash in new copy. Provides the primitive + token layer every later plan depends on.
</success_criteria>

<output>
Create `.planning/phases/07-account-library-honest-pricing-design-reconciliation-phase-a/07-01-SUMMARY.md` when done.
</output>
