---
phase: 05-distribution-operationalization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - distribution/paid-test-spec.md
autonomous: false
requirements: [GTM-03]
user_setup:
  - service: google-ads
    why: "GTM-03 names Google Search as the channel. No spend happens in this phase — the founder only needs to confirm they have (or can create) a Google Ads account, and validate the candidate keyword's volume in Keyword Planner before the test ever runs (research Environment Availability + Open Question 3). The campaign itself is BLOCKED until Phase 6 analytics is live and the organic-proof gate clears."
    dashboard_config:
      - task: "Confirm a Google Ads account exists; validate the candidate keyword volume (no spend now)"
        location: "Google Ads → Tools → Keyword Planner"

must_haves:
  truths:
    - "A paid conversion-test spec exists naming ONE specific high-intent keyword on Google Search, exact/phrase match (D-11)"
    - "The success metric is cost per completed flow (start → copy/download), tied to the v1 success metric (D-13)"
    - "A hard budget cap of €100 total is stated, with the €100 ≈ 30–70 clicks ≈ 2–10 completed flows honesty (D-14, research)"
    - "An explicit 'only after organic validation' gate is documented (D-15) with a suggested default N≈50 completed organic flows, finalized once funnel velocity is visible"
    - "The hard dependency on the Phase-6 analytics funnel (start → copy/download events) is documented — the spec is authored now but CANNOT run until Phase 6 is live (D-13, OPS-01)"
  artifacts:
    - path: "distribution/paid-test-spec.md"
      provides: "GTM-03 paid conversion-test spec: one keyword, cost-per-completed-flow metric, €100 cap, organic-proof gate, and the Phase-6 analytics dependency flagged BLOCKED"
      contains: "€100"
      min_lines: 90
  key_links:
    - from: "distribution/paid-test-spec.md"
      to: ".planning/ROADMAP.md"
      via: "spec declares a hard dependency on Phase 6 (OPS-01 / TRUST-02) analytics funnel and is marked BLOCKED until then"
      pattern: "Phase 6|Phase-6"
---

<objective>
Produce `distribution/paid-test-spec.md` (GTM-03): the paid conversion-test spec. It names one specific high-intent Google Search keyword (exact/phrase match), defines the success metric as cost per completed flow (start → copy/download), sets a hard €100 budget cap, documents an explicit "only after organic validation" gate (suggested N≈50), and flags the hard dependency on the Phase-6 analytics funnel — the spec is authored now but is BLOCKED from running until Phase 6 is live and the organic-proof gate clears.

Purpose: So the paid test is fully specified and ready to fire the moment its two preconditions (Phase-6 funnel live + organic-proof gate met) are satisfied — with honest expectations that €100 is a directional signal, not an optimization campaign.
Output: One markdown file, `distribution/paid-test-spec.md`. No app code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution-operationalization/05-CONTEXT.md
@.planning/phases/05-distribution-operationalization/05-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the paid-test spec — keyword, metric, budget, gate, and Phase-6 dependency</name>
  <files>distribution/paid-test-spec.md</files>
  <read_first>
    - .planning/phases/05-distribution-operationalization/05-CONTEXT.md (D-11…D-15 are the spec)
    - .planning/phases/05-distribution-operationalization/05-RESEARCH.md (§GTM-03: CPC benchmarks, €100 click-volume table, Candidate High-Intent Keywords, Campaign Mechanics, Conversion Tracking Architecture / Phase-6 dependency, Pitfalls 1 & 5, Open Questions 3 & 4)
    - .planning/ROADMAP.md (Phase 6 = Analytics: OPS-01 funnel events start/parse_done/letter_done/copy/download; TRUST-02 zero-PII)
    - CLAUDE.md guardrails (no income/outcome claims; "authentic" = voice not detection-evasion; nationality-neutral; zero-retention)
  </read_first>
  <action>
    Create distribution/paid-test-spec.md. Include:
    (1) "Artifacts this phase produces" — list all three Phase-5 markdown paths.
    (2) Status banner: BLOCKED until (a) Phase-6 analytics funnel is live (OPS-01 / TRUST-02) AND (b) the organic-proof gate clears (D-15). State this at the top.
    (3) Channel + keyword (D-11): Google Search only, ONE specific high-intent keyword, exact or phrase match. Recommend `german cv converter` as the exact-match candidate (highest tool-intent specificity, likely lowest CPC) per research, listing the alternates (`cv to lebenslauf`, `lebenslauf erstellen englisch`). Mark candidate volume/CPC [ASSUMED] and require validation in Google Ads Keyword Planner before launch (Open Question 3): if the chosen term has <100/mo volume, fall back to a broader phrase-match term.
    (4) Success metric (D-13): cost per completed flow, where a completed flow = start → copy/download, tied directly to the v1 success metric. State plainly that this metric REQUIRES the Phase-6 PostHog funnel to be live to measure.
    (5) Budget (D-14): hard cap €100 total, no overrun. Reproduce the research click-volume honesty: ~€1.50–€3.50/click (Germany-adjusted Career/Employment benchmark, [ASSUMED]) → €100 ≈ 30–70 clicks → ≈ 2–10 completed flows at 5–10% completion. State explicitly (Pitfall 1) that €100 answers one question — "does anyone who clicks finish the flow?" — and is a directional signal, NOT statistically significant optimization data; no ROAS / no bid optimization at this scale. Include the €5–10/day daily cap × 10–20 day window mechanic and exact-match behavior note.
    (6) Organic-proof gate (D-15, Pitfall 5): do NOT spend until the Phase-6 funnel shows a threshold of real organic completed flows. Set the spec default N = 50 completed organic flows, with an explicit note that the founder finalizes N once the funnel is live and organic velocity is visible (research Open Question 4 suggests 20–50; default to 50 for a meaningful signal).
    (7) Phase-6 analytics dependency (integration point): document the hard dependency on OPS-01 events (start, parse_done, letter_done, copy, download) and the PostHog ↔ Google Ads conversion bridge ([ASSUMED] pattern, validated in Phase 6). Cross-reference ROADMAP Phase 6.
    Apply guardrails: no income/outcome claims in any ad-copy direction; "authentic" framed as voice/quality not detection-evasion; nationality-neutral; zero-retention preserved (the landing the ad points to already states it).
    Do NOT write any task touching scanready/ source.
  </action>
  <verify>
    <automated>test -f distribution/paid-test-spec.md && grep -q "€100" distribution/paid-test-spec.md && grep -qi "cost per completed flow" distribution/paid-test-spec.md && grep -Eqi "Phase 6|Phase-6" distribution/paid-test-spec.md && grep -qi "organic" distribution/paid-test-spec.md && grep -qi "exact" distribution/paid-test-spec.md && grep -qi "Artifacts this phase produces" distribution/paid-test-spec.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - distribution/paid-test-spec.md exists with an "Artifacts this phase produces" section listing all three markdown paths.
    - A top-of-file BLOCKED status names both preconditions: Phase-6 funnel live AND organic-proof gate cleared.
    - Names ONE specific high-intent keyword on Google Search with exact/phrase match (D-11) and a "validate in Keyword Planner before launch" step.
    - Success metric = cost per completed flow (start → copy/download), stated to require the Phase-6 funnel (D-13).
    - Hard €100 cap stated (D-14) with the €100 ≈ 30–70 clicks ≈ 2–10 completed flows honesty and the explicit "directional signal, not optimization" framing (Pitfall 1).
    - Organic-proof gate documented (D-15) with default N≈50, founder-finalized once funnel velocity is visible.
    - Hard dependency on the Phase-6 analytics funnel (OPS-01 events) documented and cross-referenced to ROADMAP Phase 6.
    - Guardrail-clean: no income claims; authentic = voice not detection-evasion; nationality-neutral.
  </acceptance_criteria>
  <done>distribution/paid-test-spec.md exists and fully specifies the paid test — one keyword, cost-per-completed-flow metric, €100 cap with honest click/flow math, organic-proof gate (N≈50), and the Phase-6 BLOCKED dependency — guardrail-clean. GTM-03 artifact is complete and grounded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Founder review — keyword, metric, cap, gate, and Phase-6 dependency</name>
  <action>Pause for the founder to review the chosen keyword, the cost-per-completed-flow metric, the €100 cap, the organic-proof gate N, and the Phase-6 BLOCKED dependency — and to confirm no spend happens until both preconditions clear. No code or file changes — this is a blocking human gate.</action>
  <what-built>distribution/paid-test-spec.md — GTM-03 paid conversion-test spec: one Google Search keyword (exact/phrase match), cost-per-completed-flow metric, €100 hard cap, organic-proof gate (default N≈50), and a hard BLOCKED dependency on the Phase-6 analytics funnel.</what-built>
  <how-to-verify>
    1. Open distribution/paid-test-spec.md. Confirm the spec is marked BLOCKED until Phase-6 analytics is live AND the organic-proof gate clears — and that it does NOT instruct any spend now.
    2. Confirm you have or can create a Google Ads account, and that you understand you must validate the candidate keyword's volume in Keyword Planner before any launch (no spend needed for the check).
    3. Confirm the success metric (cost per completed flow), the €100 cap, and the organic-proof gate N (default 50) match your intent. Adjust N if you want a stricter or looser signal.
    4. Confirm you accept the honesty: €100 buys ~30–70 clicks ≈ 2–10 completed flows — a directional signal, not an optimization campaign.
  </how-to-verify>
  <resume-signal>Type "approved" once you have reviewed the keyword, metric, cap, gate N, and the Phase-6 BLOCKED dependency — or describe changes.</resume-signal>
</task>

</tasks>

<threat_model>
This phase changes ZERO app code and has no runtime/attack surface. The threat model is framed around the budget/compliance/sequencing risks of a paid-test spec, not software vulnerabilities.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → real ad spend | A spec that runs prematurely or uncapped could burn budget before validation |
| ad copy → public claims | Ad-copy framing could drift into income claims or detection-evasion framing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege (premature execution) | Running the paid test before preconditions | mitigate | Top-of-file BLOCKED status requires BOTH Phase-6 funnel live (OPS-01) and organic-proof gate (D-15, N≈50) before any spend; blocking-human checkpoint |
| T-05-07 | Denial of Service (budget burn) | €100 budget | mitigate | Hard €100 cap (D-14) + €5–10/day daily cap mechanic; framed explicitly as a directional test, no optimization spend (Pitfall 1) |
| T-05-08 | Tampering (data integrity) | Candidate keyword volume / CPC | mitigate | Volumes/CPCs labelled [ASSUMED]; Keyword Planner validation required before launch (Open Question 3); fallback to broader phrase match if <100/mo |
| T-05-09 | Repudiation/Compliance | Ad-copy direction | mitigate | Guardrails: no income/outcome claims; authentic = voice not detection-evasion; nationality-neutral; landing the ad points to already states zero-retention |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (documentation only); RESEARCH.md Package Legitimacy Audit = Not applicable |
</threat_model>

<verification>
- distribution/paid-test-spec.md exists and is ≥90 lines.
- Contains "€100", "cost per completed flow", "Phase 6"/"Phase-6", "organic", "exact", "Artifacts this phase produces", a top BLOCKED status, and the organic-proof gate N.
- Guardrail-clean: no income claims; authentic = voice not detection-evasion; nationality-neutral.
- Founder-verification checkpoint passed (keyword/metric/cap/gate/Phase-6 dependency confirmed).
</verification>

<success_criteria>
GTM-03 is satisfied: a paid conversion-test spec exists with one specific keyword, the cost-per-completed-flow success metric, the €100 hard budget cap, and an explicit "only after organic validation" gate — plus the honest framing (directional signal, not optimization) and the documented hard dependency on the Phase-6 analytics funnel (BLOCKED until live + gate cleared).
</success_criteria>

<output>
Create `.planning/phases/05-distribution-operationalization/05-03-SUMMARY.md` when done.
</output>
