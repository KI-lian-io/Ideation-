---
phase: 04-design-pass-vercel-deploy
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - scanready/vercel.json
  - scanready/src/app/api/cover-letter/route.ts
autonomous: false
requirements: [OPS-02, OPS-03]
user_setup:
  - service: vercel
    why: "Public hosting for the app; production env var + Node runtime provisioning live in the Vercel dashboard, not in committed code"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Set in Vercel → Project → Settings → Environment Variables (Production), server-only (NOT a NEXT_PUBLIC_ var)"
    dashboard_config:
      - task: "Connect the GitHub repo / import the project and deploy (push-to-deploy), or run the Vercel CLI"
        location: "Vercel dashboard → Add New → Project"
      - task: "Confirm Node.js version is 20.x"
        location: "Vercel → Project → Settings → General → Node.js Version"
must_haves:
  truths:
    - "The app is publicly reachable on a Vercel default subdomain (e.g. *.vercel.app)"
    - "The cover-letter Anschreiben streams to completion in production without hitting the function time limit (no FUNCTION_INVOCATION_TIMEOUT)"
    - "ANTHROPIC_API_KEY is configured in the Vercel production environment and stays server-only"
    - "Node is pinned to 20.x on Vercel and maxDuration is declared consistently for the cover-letter function"
  artifacts:
    - path: "scanready/vercel.json"
      provides: "Function maxDuration config (cover-letter 300, parse 60) kept in sync with the route segment config"
      contains: "maxDuration"
    - path: "scanready/src/app/api/cover-letter/route.ts"
      provides: "maxDuration bumped 120 → 300 (Hobby hard ceiling), streaming + grounding logic UNCHANGED (D-05)"
      contains: "maxDuration = 300"
  key_links:
    - from: "scanready/vercel.json"
      to: "scanready/src/app/api/cover-letter/route.ts"
      via: "vercel.json functions[].maxDuration must equal the route's export const maxDuration (300) — drift causes premature timeout (RESEARCH Pitfall 4)"
      pattern: "maxDuration"
---

<objective>
Deploy the working, branded app publicly to Vercel (Hobby tier, default subdomain — D-21/D-22) with `ANTHROPIC_API_KEY` provisioned in production and Node pinned to 20.x (D-23/OPS-03), and prove the Opus + adaptive-thinking cover-letter stream completes inside the function time limit (OPS-02/SC4). Bump the cover-letter `maxDuration` from 120 to 300 (the confirmed Hobby hard ceiling) in BOTH the route file and a new `vercel.json`, kept in sync to avoid the override-drift pitfall. Because RESEARCH rated the "does Opus fit in 300s" question LOW confidence — it can only be settled by a real deploy test — this plan includes an explicit measurement step against Vercel Function Logs and the documented fallback (switch the cover-letter model to `claude-sonnet-4-6`, or upgrade to Pro for the 800s ceiling) if P95 exceeds ~280s.

Purpose: OPS-02 + OPS-03 — make the app a live URL whose core streaming feature does not time out, with the production secret in place. Per D-05 the backend logic is frozen — the only route change is the timeout ceiling, not the streaming/grounding behavior.
Output: A public Vercel URL serving the landing + tool, with the cover-letter stream verified to complete within the limit (or the fallback applied and re-verified).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-pass-vercel-deploy/04-CONTEXT.md
@.planning/phases/04-design-pass-vercel-deploy/04-RESEARCH.md
@scanready/AGENTS.md
@scanready/src/app/api/cover-letter/route.ts
@scanready/src/app/api/parse/route.ts
@scanready/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy config — bump cover-letter maxDuration 120→300, create vercel.json in sync, verify production build</name>
  <read_first>
    - scanready/src/app/api/cover-letter/route.ts (line 6: `export const maxDuration = 120` — the only line that changes; runtime/stream/grounding logic stays)
    - scanready/src/app/api/parse/route.ts (line 6: `maxDuration = 60` — stays as-is)
    - scanready/package.json (engines already `>=20.9.0`; `.nvmrc` is `20`; lint script calls eslint directly)
    - 04-RESEARCH.md §"vercel.json — Correct Pattern" (functions block, keep route + vercel.json in sync), §"Confirmed Hobby Ceiling" (300s hard max), §Pitfall 3 (Turbopack `--webpack` fallback), §Pitfall 4 (maxDuration drift)
    - 04-CONTEXT.md D-22 (Hobby, maxDuration sized to ceiling, Node 20.x), D-05 (zero backend logic change)
  </read_first>
  <action>
    In `scanready/src/app/api/cover-letter/route.ts` change ONLY `export const maxDuration = 120` to `export const maxDuration = 300` (D-22 — Hobby hard ceiling). Make no other change to this file — the `text_delta`-only streaming filter and grounding behavior are frozen (D-05/D-10). Create `scanready/vercel.json` with a `functions` block setting `src/app/api/cover-letter/route.ts` → `maxDuration: 300` and `src/app/api/parse/route.ts` → `maxDuration: 60`, matching the route segment configs exactly (RESEARCH Pitfall 4 — drift causes premature timeout). Do NOT add a `maxDuration` value that disagrees with the route files. Run `npm run build` to confirm the production build succeeds with the route split + landing from Plans 01/02; if it fails citing a webpack config from a transitive dependency (RESEARCH Pitfall 3), add `"build": "next build --webpack"` to `package.json` scripts and re-run — otherwise leave the build script unchanged. Do NOT add a `NEXT_PUBLIC_POSTHOG_KEY` or any PostHog/analytics config (PostHog is DEFERRED to Phase 6 — D-23).
  </action>
  <verify>
    <automated>cd scanready && grep -q 'maxDuration = 300' src/app/api/cover-letter/route.ts && test -f vercel.json && grep -q '"maxDuration": 300' vercel.json && grep -q '"maxDuration": 60' vercel.json && grep -q '"node": ">=20.9.0"' package.json && ! grep -rq 'NEXT_PUBLIC_POSTHOG' src/ vercel.json && npm run build && echo DEPLOY_CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cover-letter/route.ts` declares `maxDuration = 300` with no other logic change; `parse/route.ts` stays at 60.
    - `vercel.json` exists with a `functions` block: cover-letter `maxDuration: 300`, parse `maxDuration: 60` — exactly matching the route files (no drift).
    - No PostHog/analytics env var or config is introduced; `engines.node` stays `>=20.9.0`.
    - `npm run build` succeeds; verification prints `DEPLOY_CONFIG_OK`.
  </acceptance_criteria>
  <done>Deploy config is committed and the production build is green; the function time budget is set to the Hobby ceiling consistently across both config surfaces.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Human deploys to Vercel + provisions ANTHROPIC_API_KEY (Production) + confirms Node 20.x</name>
  <action>Human-only Vercel dashboard work — see how-to-verify. Provision the production secret and Node version, deploy, and return the live URL. Claude cannot do this from committed code (the secret must never be committed — D-23).</action>
  <what-built>The app builds for production with `vercel.json` (cover-letter `maxDuration: 300`) and Node pinned to 20.x. It is ready to deploy, but Vercel project creation, the production `ANTHROPIC_API_KEY`, and the Node-version confirmation can only be done by a human in the Vercel dashboard (no committed-code path, and the secret must not be committed — D-23).</what-built>
  <how-to-verify>
    1. Create/import the project on Vercel: dashboard → Add New → Project → connect the GitHub repo (branch `claude/ecom-low-barrier-tools-zpyb10`) with the project root set to `scanready/`, OR run the Vercel CLI from `scanready/`. Use the default `*.vercel.app` subdomain (custom domain is Phase 5 — D-21).
    2. In Vercel → Project → Settings → Environment Variables, add `ANTHROPIC_API_KEY` for the Production environment as a server-only variable (do NOT prefix with `NEXT_PUBLIC_`). Do NOT add any PostHog key (Phase 6 — D-23).
    3. In Vercel → Project → Settings → General → Node.js Version, confirm it is 20.x (Vercel reads `engines.node`; set it explicitly if not auto-detected).
    4. Trigger a production deploy and confirm it succeeds. Paste the resulting public `*.vercel.app` URL here.
    5. Visit the URL: confirm the landing renders at `/` and the tool loads at `/app`.
  </how-to-verify>
  <resume-signal>Reply with the live `*.vercel.app` URL and confirm `ANTHROPIC_API_KEY` is set (Production) + Node is 20.x, or describe any deploy error to debug.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: SC4 verification — measure live cover-letter stream wall-clock, apply documented fallback if over budget</name>
  <read_first>
    - 04-RESEARCH.md §"Will Opus 4.8 + Adaptive Thinking Complete Under 300 s?" (LOW confidence — measure, don't assume), §"Open Items" item 1 (measure in Function Logs; if P95 > 280s switch to claude-sonnet-4-6 or upgrade to Pro), §"SSE / ReadableStream Survival on Vercel"
    - scanready/src/lib/anthropic.ts (GENERATION_MODEL = claude-opus-4-8; the cost lever is the documented Sonnet switch — read before any model change)
    - 04-CONTEXT.md D-22 (confirm the stream fits the Hobby ceiling during deploy), D-05 (any model change is config in anthropic.ts, not pipeline logic)
  </read_first>
  <action>
    Against the live deployment from the checkpoint, run the full flow end-to-end with a representative real CV + a real job posting + answers, and generate an Anschreiben. Measure the cover-letter function's wall-clock duration in Vercel → Project → Functions / Logs (or Observability) for `/api/cover-letter`. Run it at least 3 times (cold start + warm) to get a realistic spread; record the max observed duration as the P95 proxy. Confirm the stream reaches completion in the browser (the full letter renders, no `FUNCTION_INVOCATION_TIMEOUT` / 504). PASS condition (SC4): the stream completes and the max observed duration is comfortably under 300s (target ≤ ~280s headroom). If the max observed duration exceeds ~280s OR any run times out: apply the RESEARCH-documented fallback — switch the cover-letter model to `claude-sonnet-4-6` in `src/lib/anthropic.ts` (`GENERATION_MODEL`, the pre-documented cost/speed lever; pipeline logic stays unchanged per D-05), redeploy, and re-measure until the stream completes under budget. If Sonnet still does not fit, document the Pro-tier upgrade (800s ceiling — D-22) as the escalation in the SUMMARY and surface it to the user rather than silently leaving a timing risk. Record the measured durations, the chosen model, and the final disposition in `04-03-SUMMARY.md`. Do NOT claim SC4 is met on assumption — the SUMMARY must cite real measured numbers from the Function Logs.
  </action>
  <verify>
    <human-check>
      In Vercel Function Logs for `/api/cover-letter` on the live deployment: at least 3 cover-letter generations completed without a 504 / FUNCTION_INVOCATION_TIMEOUT, the full letter rendered in the browser each time, and the max observed wall-clock duration is recorded and under the function limit. If a fallback was applied, the post-fallback runs also completed under budget. The measured numbers + chosen model are written into 04-03-SUMMARY.md.
    </human-check>
  </verify>
  <acceptance_criteria>
    - At least 3 live cover-letter generations completed without timeout, full letter rendered each time.
    - The max observed `/api/cover-letter` wall-clock duration is recorded from Vercel Function Logs and is under the 300s limit (target ≤ ~280s).
    - If over budget, the documented fallback (Sonnet model switch, then Pro escalation if needed) was applied and re-measured; the final model + disposition is recorded.
    - `04-03-SUMMARY.md` cites the real measured durations — SC4 is not claimed on assumption.
  </acceptance_criteria>
  <done>SC4 is settled with evidence: the cover-letter stream provably completes within the Vercel function limit on the live deployment (with the fallback applied and re-verified if the Opus default exceeded budget).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `scanready/vercel.json` [NEW — `functions` block: cover-letter `maxDuration: 300`, parse `maxDuration: 60`]
- `scanready/src/app/api/cover-letter/route.ts` [CHANGED — single line: `maxDuration` 120 → 300; streaming/grounding logic unchanged per D-05]
- Possibly `scanready/package.json` [CHANGED only if Turbopack build needs the `next build --webpack` fallback — RESEARCH Pitfall 3]
- Possibly `scanready/src/lib/anthropic.ts` [CHANGED only if the SC4 fallback fires — `GENERATION_MODEL` → `claude-sonnet-4-6`, the pre-documented cost/speed lever]
- A live `*.vercel.app` deployment (external artifact) with `ANTHROPIC_API_KEY` provisioned in production and Node pinned to 20.x
- Measured cover-letter stream durations recorded in `04-03-SUMMARY.md`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/cover-letter, /api/parse | Untrusted CV/job/answers text crosses here; length guards from Phase 3 remain in force (unchanged) |
| repo/commit → Vercel production env | The production `ANTHROPIC_API_KEY` lives only in the Vercel dashboard, never in committed code |
| Vercel function → Anthropic API | The server-only key authorizes outbound model calls |

## STRIDE Threat Register (ASVS L1, proportionate)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information disclosure | `ANTHROPIC_API_KEY` | mitigate | Provisioned as a server-only Vercel env var (NOT `NEXT_PUBLIC_`), accessed only in `src/lib/anthropic.ts` server-side; never bundled to the client. The checkpoint explicitly instructs the non-`NEXT_PUBLIC_` setting; verify grep finds no `NEXT_PUBLIC` key reference. |
| T-04-05 | Denial of service | /api/cover-letter (300s function) | mitigate | Phase-3 input-length guards (30k/15k/2k) bound model input and cost; `maxDuration: 300` caps any single invocation. SC4 measurement confirms normal runs finish well under the cap. |
| T-04-06 | Tampering | Vercel deploy pipeline | accept | Push-to-deploy from the project branch; no secrets in the repo; Vercel build is the trusted boundary. Low-value target, no PII persisted. |
| T-04-SC | Tampering | npm installs | mitigate | No new npm packages in this plan. If the Turbopack fallback is needed it only adds a `--webpack` flag to an existing script — no dependency added. Package legitimacy gate N/A. |

No DB / auth / accounts / session persistence exists (zero-retention guardrail) — no injection/authz/session threats apply beyond the input-length guards already shipped in Phase 3.
</threat_model>

<verification>
- `npm run build` passes locally with `vercel.json` and the 300s route config.
- The live `*.vercel.app` URL serves the landing at `/` and the tool at `/app`.
- At least 3 live cover-letter generations complete without a 504, with max wall-clock duration recorded under the function limit (fallback applied + re-verified if needed).
- `ANTHROPIC_API_KEY` is set in Vercel Production (server-only) and Node is 20.x.
</verification>

<success_criteria>
- OPS-03 met: production env configured (`ANTHROPIC_API_KEY`) and Node pinned to 20.x on Vercel; PostHog correctly deferred (D-23).
- OPS-02 / SC4 met: app publicly reachable on a Vercel URL and the cover-letter stream provably completes within the function time limit (`maxDuration` declared in `vercel.json`, tier = Hobby confirmed) — backed by real measured durations, not assumption.
- No backend logic change beyond the timeout ceiling (and the documented model lever only if the fallback fires) — D-05 honored.
</success_criteria>

<output>
Create `.planning/phases/04-design-pass-vercel-deploy/04-03-SUMMARY.md` when done, including the measured cover-letter stream durations and the final model/tier disposition.
</output>
