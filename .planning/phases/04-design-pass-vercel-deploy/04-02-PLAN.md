---
phase: 04-design-pass-vercel-deploy
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - scanready/src/app/page.tsx
  - scanready/src/app/globals.css
  - scanready/public/og-image.svg
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "A visitor at / sees a conversion-oriented marketing landing that communicates zero-retention, grounded output, and norm-correct German format — trust-forward, not generic AI-purple"
    - "The hero headline 'Win the 8-second German recruiter scan.' renders in serif with the single blue hairline accent, and every CTA links to /app"
    - "All six locked sections render in order (hero, two-column pathways, how-it-works, trust band, founder note, proof strip) and read on a 375px-wide mobile screen"
    - "Static before→after mockups convey the product (no live demo on /), led by the founder's own dogfooded example"
    - "No banned copy appears: no detection-evasion ('looks AI', 'beat the filter'), no income/'get hired'/guarantee claims; the bilingual 'explains every change' claim is scoped to the Lebenslauf only"
    - "An OG card asset exists so the deployed link is not naked"
  artifacts:
    - path: "scanready/src/app/page.tsx"
      provides: "Marketing landing — Server Component, 6 sections, static metadata, nav, footer, static mockups, scroll micro-motion"
      min_lines: 200
      contains: "Win the 8-second German recruiter scan"
    - path: "scanready/public/og-image.svg"
      provides: "Minimal OG card (wordmark + headline + trust line) referenced by metadata"
  key_links:
    - from: "scanready/src/app/page.tsx"
      to: "scanready/src/app/app/page.tsx"
      via: "every CTA is an <a href=\"/app\"> into the tool route (D-02 — no embedded live demo)"
      pattern: "href=\"/app\""
    - from: "scanready/src/app/page.tsx"
      to: "scanready/src/app/globals.css"
      via: "serif headings via font-[--font-serif] / font-serif and the blue-700 accent token"
      pattern: "font-.*serif|font-serif"
---

<objective>
Replace the `/` placeholder with the full conversion-oriented marketing landing — a Server Component rendering the locked six-section blueprint (D-15) with serif headings, the rationed deep-blue trust accent, static hand-authored before→after mockups (D-16) led by the founder's dogfooded example, restrained scroll micro-motion (D-13), a minimal OG card (D-20), and a full WCAG-AA pass (D-19). Every CTA routes to the tool at `/app` (D-02 — no live demo embedded on `/`). All copy obeys the hard guardrails (D-06/D-07/D-10): authentic = quality/voice (never detection-evasion), capability not outcome, bilingual change-notes scoped to the Lebenslauf only.

Purpose: This is UI-01 — the landing must make the trust case (zero-retention, grounded, norm-correct) credibly to an international applying in Germany, in a "paper-credential" aesthetic that is explicitly NOT AI-purple. It is the conversion front door to the already-working tool.
Output: A static, SEO-ready, mobile-readable landing at `/` plus its OG asset, ready to deploy in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-pass-vercel-deploy/04-CONTEXT.md
@.planning/phases/04-design-pass-vercel-deploy/04-UI-SPEC.md
@.planning/phases/04-design-pass-vercel-deploy/04-RESEARCH.md
@scanready/AGENTS.md
@scanready/src/app/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing scaffold — Server Component, metadata, nav, hero, footer, all six section shells in order</name>
  <read_first>
    - scanready/src/app/page.tsx (the Plan-01 placeholder Server Component this replaces)
    - scanready/src/app/globals.css (the `--font-serif` and `--color-accent` tokens + reduced-motion guard from Plan 01)
    - 04-UI-SPEC.md §"C. Routing split" (landing = Server Component, no 'use client'), §"D. Navigation bar", §"E. Six-section landing layout" (Sections 1-6 exact markup + copy), §"#### Footer", §"Copywriting Contract" (LOCKED copy table)
    - scanready/AGENTS.md then node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-metadata.md (metadata is Server-Component-only — landing must NOT have 'use client')
  </read_first>
  <action>
    Replace `scanready/src/app/page.tsx` with the marketing landing as a Server Component — do NOT add `'use client'` (metadata export requires a Server Component per RESEARCH). Export `const metadata: Metadata` with the page title/description from UI-SPEC §Copywriting and `openGraph.images: [{ url: "/og-image.svg", width: 1200, height: 630 }]`. Build the page structure as `<nav>` (sticky, per UI-SPEC §D — wordmark "ScanReady" + blue "DE" accent + single "Try it free" CTA to `/app`), then `<main>` containing the six `<section>` elements IN ORDER per UI-SPEC §E, then `<footer>` per UI-SPEC. Each `<section>` uses a descriptive `aria-labelledby` pointing to its `<h2>` (hero uses `<h1>`); alternate backgrounds `bg-white` (1/3/5) and `bg-zinc-100` (2/4/6). Use serif on the hero `<h1>` and every section `<h2>` via the `font-[--font-serif]` / `font-serif` utility at `font-semibold` (NEVER `font-bold`). Implement the hero (Section 1) fully now: eyebrow "For internationals applying in Germany", the LOCKED headline "Win the 8-second German recruiter scan." with the blue hairline `border-b-2 border-blue-700` accent span around "8-second" (`aria-hidden`), the D-08 subhead (bilingual claim scoped to "every Lebenslauf change"), the primary CTA "Convert your CV — it's free" → `/app`, and the zero-retention micro-trust line with an inline lock SVG. Implement the footer fully. Stub Sections 2-6 as titled `<section>` shells (correct heading + copy + CTA from the copy table) — their mockups/details land in Tasks 2-3. Reserve the deep-blue accent (`blue-700`) to ONLY the five UI-SPEC contexts. Add `focus-visible:outline-2 focus-visible:outline-blue-700 focus-visible:outline-offset-2` to every CTA link.
  </action>
  <verify>
    <automated>cd scanready && ! head -1 src/app/page.tsx | grep -q "'use client'" && grep -q 'export const metadata' src/app/page.tsx && grep -q 'Win the 8-second German recruiter scan' src/app/page.tsx && grep -q 'For internationals applying in Germany' src/app/page.tsx && test "$(grep -c '<section' src/app/page.tsx)" -ge 6 && grep -q '<nav' src/app/page.tsx && grep -q '<footer' src/app/page.tsx && grep -q 'border-b-2 border-blue-700' src/app/page.tsx && grep -q 'href="/app"' src/app/page.tsx && npx tsc --noEmit && echo LANDING_SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` is a Server Component (no `'use client'`) exporting `metadata` with the UI-SPEC title/description and `openGraph.images` referencing `/og-image.svg`.
    - `<nav>`, `<main>` with ≥6 `<section>` elements in the locked order, and `<footer>` are present; backgrounds alternate per §E.
    - Hero renders the locked headline with the single blue hairline accent, the D-08 subhead, the CTA to `/app`, and the zero-retention micro-line; serif headings use `font-semibold` (no `font-bold`).
    - Every CTA links to `/app` and has a visible focus outline; `npx tsc --noEmit` passes; verification prints `LANDING_SCAFFOLD_OK`.
  </acceptance_criteria>
  <done>The landing renders all six sections in order with a complete hero, nav, and footer; the page is a static SEO-ready Server Component routing into the tool.</done>
</task>

<task type="auto">
  <name>Task 2: Section bodies + static before→after mockups (D-16) — two-column pathways, how-it-works, trust band, founder note, proof strip</name>
  <read_first>
    - scanready/src/app/page.tsx (the scaffold from Task 1 with stubbed Sections 2-6)
    - 04-UI-SPEC.md §"E. Section 2-6" (full markup), §"G. Static before/after mockups" (HTML/CSS technique, dimensions, exact mockup specs + accessibility), §"Copywriting Contract" (LOCKED copy per element), §"Color" (accent rationing — blue dot pins, founder signature bar)
    - 04-CONTEXT.md D-09 (two-column), D-16/D-17 (founder dogfood proof, no fake counters/testimonials), D-10 (Lebenslauf-only change notes)
  </read_first>
  <action>
    Fill in Sections 2-6 with the locked copy and the three static mockups (HTML/CSS per UI-SPEC §G — no `<img>`, no external assets). Section 2 (two-column pathways, D-09): LEFT card "Sounds like you — not a template." with the personalization-questions framing + the authentic before→after mockup (struck-through `line-through italic` generic filler in zinc-400 → grounded specific German line in zinc-900); RIGHT card "The format gap, closed." with the nationality-neutral Chancenkarte reframe, the native-speaker-review nudge, and the mini-Lebenslauf mockup whose norm-gap pins are blue-700 dots (the 4th reserved accent use); single shared CTA beneath. Section 3 (how-it-works): three grounded steps with the locked copy + repeated CTA. Section 4 (trust band): blue-700 lock badge (reuse the inline lock SVG), "Zero-retention, by design." copy, three inline checkmark trust points. Section 5 (founder note): the `w-8 h-0.5 bg-blue-700` signature bar (5th reserved accent use), first-person dogfooding copy, "— Kilian Hartmann" attribution; render the optional build-in-public link ONLY if a URL is supplied (otherwise omit — D-17). Section 6 (proof strip): eyebrow "Real output — the founder's own application" + the full before→after founder mockup (mark placeholder content with a `REPLACE WITH REAL CONTENT` HTML comment if the real excerpt is unavailable) + final CTA + sub-copy. Every mockup container gets `role="img"` and the exact `aria-label` from UI-SPEC §E/§G; decorative faux-content lines get `aria-hidden="true"`. Use `text-sm uppercase tracking-widest font-semibold` for all eyebrows/labels — never `text-xs`. Do NOT introduce any fabricated counter, "join N", testimonial, or logo wall (D-17).
  </action>
  <verify>
    <automated>cd scanready && grep -q 'Two documents. Both have to survive the scan' src/app/page.tsx && grep -q 'The format gap, closed' src/app/page.tsx && grep -q 'native German speaker should review' src/app/page.tsx && grep -q 'Zero-retention, by design' src/app/page.tsx && grep -q 'Built by someone who needed it' src/app/page.tsx && grep -q 'Real output — the founder' src/app/page.tsx && test "$(grep -c 'role="img"' src/app/page.tsx)" -ge 3 && grep -q 'w-8 h-0.5 bg-blue-700' src/app/page.tsx && ! grep -qi 'text-xs' src/app/page.tsx && npx tsc --noEmit && echo SECTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Sections 2-6 render their locked headings, copy, and CTAs; three static mockups (HTML/CSS) are present, each with `role="img"` + descriptive `aria-label`, decorative lines `aria-hidden`.
    - Norm-gap pins and the founder signature bar use `blue-700`; no accent appears outside the five reserved contexts.
    - No fabricated counters/testimonials/logo walls; no `text-xs` (all labels at `text-sm` per D-19); the bilingual change-note claim names the Lebenslauf only.
    - `npx tsc --noEmit` passes; verification prints `SECTIONS_OK`.
  </acceptance_criteria>
  <done>All six sections are content-complete with on-brand static proof mockups; the landing tells the full trust + value story with no live demo and no fabricated proof.</done>
</task>

<task type="auto">
  <name>Task 3: Scroll micro-motion, OG card asset, and copy-guardrail + WCAG-AA final sweep</name>
  <read_first>
    - scanready/src/app/page.tsx (the content-complete landing from Tasks 1-2)
    - scanready/src/app/globals.css (the `prefers-reduced-motion` guard added in Plan 01 — confirm it covers the new animation)
    - 04-UI-SPEC.md §"H. Motion contract" (fade + 8px slide-up on scroll via IntersectionObserver, NOT on hero; CTA hover lift; no parallax/gradient/particle/video; no scroll-animation library), §"Copywriting Contract → Copy prohibitions", §"Accessibility → Landing page" (contrast, focus, landmarks, mockup alt, reduced-motion)
    - 04-CONTEXT.md D-06/D-07/D-10 (banned phrases), D-13 (motion), D-20 (minimal OG card)
  </read_first>
  <action>
    Add the restrained scroll micro-motion (D-13/UI-SPEC §H): default section state `opacity-0 translate-y-2`, animated to `opacity-100 translate-y-0` on scroll-into-view via an `IntersectionObserver` (`threshold: 0.1`). Because the landing is a Server Component, implement the observer as a tiny inline `<script>` (vanilla JS, no library — CLAUDE.md "What NOT to Add") that toggles a class, OR factor a minimal `'use client'` child wrapper component used only for the observer — keep the page itself a Server Component for SEO/metadata. Do NOT animate the hero (first paint). Add the CTA hover lift (`hover:-translate-y-0.5 transition-transform duration-150`) to the blue CTA buttons. Confirm the `prefers-reduced-motion: reduce` guard in `globals.css` disables these (animated elements must remain readable in their static end state). Create `scanready/public/og-image.svg` — a minimal 1200×630 OG card per D-20: wordmark "ScanReady", the headline "Win the 8-second German recruiter scan.", and a one-line trust line ("Zero-retention. Grounded only in your real facts."), light surface + the deep-blue accent, no fabricated content. Final guardrail sweep: grep the landing for the D-06/D-07 banned phrases and remove any that slipped in. Final WCAG-AA sweep: confirm no readable text uses `text-zinc-400` (decorative/placeholder only), all CTAs have a focus outline, headings descend `h1→h2→h3` with no skip, and `<main>` is present.
  </action>
  <verify>
    <automated>cd scanready && test -f public/og-image.svg && grep -q 'IntersectionObserver' src/app/page.tsx && grep -q 'translate-y' src/app/page.tsx && grep -qi 'zero-retention' public/og-image.svg && ! grep -Eiq 'rejected as ai|looks ai-generated|beat the filter|pass the ats|get hired|land a job|job guarantee|success rate|humanize' src/app/page.tsx && grep -q 'prefers-reduced-motion' src/app/globals.css && grep -q '<main' src/app/page.tsx && npx tsc --noEmit && npm run build && echo MOTION_OG_SWEEP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `public/og-image.svg` exists (1200×630, wordmark + headline + trust line, no fabricated content) and is referenced by the page metadata.
    - Scroll micro-motion uses `IntersectionObserver` with no library; the hero is not animated; CTA hover lift is present; the `prefers-reduced-motion` guard disables motion.
    - No banned D-06/D-07 phrase appears in the landing; no readable text uses `text-zinc-400`; `<main>` and a non-skipping heading hierarchy are present.
    - `npx tsc --noEmit` and `npm run build` both pass; verification prints `MOTION_OG_SWEEP_OK`.
  </acceptance_criteria>
  <done>The landing has earned, reduced-motion-safe micro-motion, a deployable OG card, and is verified clean of banned copy and AA contrast/structure gaps; the production build succeeds.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `scanready/src/app/page.tsx` [REWRITTEN — the full marketing landing Server Component: nav, six sections, footer, three static HTML/CSS mockups, IntersectionObserver scroll motion, static metadata + openGraph]
- `scanready/public/og-image.svg` [NEW — minimal OG card asset referenced by metadata]
- `scanready/src/app/globals.css` [TOUCHED only if the reduced-motion guard needs to cover the new animation class — keyframes may be added here]
- Possibly one minimal `'use client'` observer wrapper component (builder's discretion) if the inline-script approach is not used — keeps the page a Server Component
- New CTA/section utility classes consuming the Plan-01 `--font-serif` / `blue-700` accent tokens
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → static html (`/`) | The landing is hand-authored static marketing content — no user input is rendered |
| visitor → /app | CTAs are plain `<a href="/app">` navigations; no data crosses from the landing |

## STRIDE Threat Register (ASVS L1, proportionate)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering (XSS) | landing page.tsx / og-image.svg | mitigate | All content is hand-authored static markup; no `dangerouslySetInnerHTML`, no user input, no remote data fetch. The OG SVG is static hand-authored content. |
| T-04-03 | Information disclosure | static landing | accept | The landing contains only public marketing copy and the founder's own dogfooded example — no PII, no secrets, nothing server-only. |
| T-04-SC | Tampering | npm installs | mitigate | No new npm packages — scroll motion uses native `IntersectionObserver` (no library per CLAUDE.md "What NOT to Add"); Lora was wired in Plan 01 via `next/font/google`. Package legitimacy gate N/A. |

No API route, DB, auth, or env access is touched by this plan — the landing reaches the backend only via a navigation link, so no injection/authz/session threats apply.
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` pass with the landing in place.
- `npm run dev` shows all six sections in order at `/`, readable at 375px width, with CTAs into `/app` and no AI-purple aesthetic.
- All grep gates print their OK sentinels; the banned-phrase grep returns no matches.
</verification>

<success_criteria>
- UI-01 met: trust-forward, German-market-credible landing communicating zero-retention + grounded + norm-correct, not generic AI-purple.
- Six locked sections in order, mobile-readable, static mockups only (no live demo — D-02), founder-dogfood proof only (no fabrication — D-17).
- Copy obeys D-06/D-07/D-10; motion obeys D-13; accessibility obeys D-19; OG card exists per D-20.
</success_criteria>

<output>
Create `.planning/phases/04-design-pass-vercel-deploy/04-02-SUMMARY.md` when done.
</output>
